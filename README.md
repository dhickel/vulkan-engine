# vulkan-engine

Documentation entrypoints for the current refactor:
- API path: `docs/api/00-index.md`
- Internal contributor path: `docs/internal/00-index.md`

Runtime migration note:
- Root binary (`cargo run`) prints guidance and exits.
- Use renderer examples under `src/renderer/examples/` for runtime behavior.
