# Editor Asset Browser and Wall Chunk Placement

## Status

This page is historical background for an earlier editor placement slice. The current workspace no longer declares an editor app crate, so this document is not a live launch guide and should not be used as validation guidance for the app-owned loop primitives sprint.

For current project/package validation examples, use [Packaging CLI](10-packaging-cli.md). For the current custom app proof, use [Dogfood Vertical Slice](14-dogfood-vertical-slice.md). For the root project launcher, use [Runtime Project Launcher](11-runtime-project-launcher.md).

## Historical contract summary

The retired editor slice demonstrated package-backed asset browser placement, durable scene node identity, save/reload, and headless draw capture validation. The durable design remains useful context, but live commands must be taken from the current docs linked above.
