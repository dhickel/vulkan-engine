[package]
name = "engine_pack"
version = "0.1.0"
edition = "2021"

[dependencies]
renderer = { path = "../../src/renderer" }
launch_shared = { path = "../../src/launch_shared" }
package_io = { path = "../../src/package_io" }
serde = { version = "=1.0.224", features = ["derive"] }
serde_json = "=1.0.145"
toml = "=0.8.23"
bsp = { path = "../../src/bsp" }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
bsp_generator = { path = "../../src/bsp_generator" }
