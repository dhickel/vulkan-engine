---
schema_version: 1
document_type: api-specification
status: active
owner: api
created: 2026-07-03
---

# API Specification

## Active Contracts

| id | route_or_surface | status | intended_contract | payload_or_status | compatibility_rule | validation | related_decisions | related_knowledge |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| API-20260703-01 | Renderer examples | active | Renderer examples are canonical runtime entrypoints for smoke validation and visual capture validation. | `src/renderer/examples/*.rs`, root `AGENTS.md` | Do not break documented example commands without updating root guidance and relevant docs. | `cargo check -p renderer --examples` and timeout-bound smoke/capture commands. | none | none |
| API-20260723-01 | Renderer startup preload configuration | active | `RendererConfig::startup_model_path` optionally selects the renderer-owned startup/debug model used when `preload_startup_scene` is true. `None` preserves the built-in DamagedHelmet default; `Some(path)` loads that model instead. `preload_startup_scene = false` disables startup scene preload regardless of `startup_model_path`. | `src/renderer/src/api/config.rs`, `src/renderer/src/api/renderer.rs`, `src/renderer/src/vulkan/vk_render.rs`, `src/renderer/src/scene/debug_scenarios.rs` | This is a narrow diagnostic/early-visualization preload selector, not root project scene selection. Keep app-owned scenes free to set `preload_startup_scene = false`. | `cargo check`, `cargo test -p renderer`, `cargo check -p renderer --examples`, affected app checks, guide checkpoint check. | none | none |
| API-20260703-02 | Headless capture flags | active | Capture-enabled examples write durable PNG/JSON sidecar evidence under task-scoped `.internal-dev/captures/` paths when visual proof is required. | capture examples, capture skill | Sidecar metadata must identify status, target, actual execution frame, requested frame when available, source, format, and extent when available. | Headless capture skill validation. | none | none |
| API-20260703-03 | Runtime launch registry | active | This file keeps the current list of app/example launch commands, runtime arguments, and scene/project entrypoints used for manual testing and regression validation. | `src/launch.rs`, `apps/dungeon_dogfood/src/main.rs`, `apps/voxel_demo/src/main.rs`, `apps/voxel_demo/src/cli.rs`, `src/renderer/examples/common/mod.rs`, `tools/engine_pack/src/main.rs` | When adding, removing, or changing a runtime flag, app binary, scene/project manifest, or reusable launch path, update the Runtime Launch Registry below in the same change. | `cargo check` for touched packages plus focused runtime smoke/headless capture where practical. | `DECISION-20260703-01` | none |
| API-20260706-02 | Renderer camera look-at facade | active (compatibility/capture facade) | `Renderer::set_camera_look_at(eye, target, up)` applies the renderer-owned camera position and orientation used by frame rendering and headless captures. Invalid look-at vectors return `RendererError::InvalidState` through the existing renderer API error surface and do not silently apply false camera metadata. | `src/renderer/src/api/renderer.rs`, `src/renderer/src/data/camera.rs` | Keep this as a narrow compatibility/capture facade over existing camera behavior; for custom apps, prefer app-owned `Camera` state and `CameraView`. Do not change renderer/Vulkan frame submission or scene-camera precedence without updating the renderer camera behavior knowledge note. | `cargo test -p renderer camera -- --nocapture`, `cargo check -p renderer --examples`, and headless draw captures when used for visual proof. | none | `renderer-camera-override-behavior.md` |
| API-20260707-01 | App-owned input action bridge and renderer routing | active | `engine::input::InputActionEventEmitter` emits action events from a caller-provided `InputSnapshot` and frame index into a caller-owned `EventBus`; `Renderer::route_platform_input` handles renderer platform side effects and returns `RendererInputRouting`; `engine::input::queue_routed_input_event` queues uncaptured input into caller-owned `InputSystem`. | `src/input.rs`, `src/renderer/src/api/renderer.rs`, `src/renderer/src/api/mod.rs`, `src/renderer/src/api/prelude.rs` | `Renderer::update_input` remains compatibility behavior that composes routing with renderer-owned input queueing. Do not make renderer depend on root `engine`. | `cargo test -p engine`, `cargo test -p renderer`, `rg -n "dispatch_frame\\(|emit_input_action|InputActionEventEmitter|update_input\\(|DeviceEvent::MouseMotion" src apps tests`. | `DECISION-20260707-01` | none |
| API-20260721-01 | Renderer cursor confinement policy | active | FPS cursor confinement is an edge-triggered persistent winit request. Pointer leave/enter does not release and recreate the request; genuine ImGui/debug-UI policy changes are deferred while the pointer is outside the window and applied after entry. App loops that intercept UI registration hotkeys call `Renderer::refresh_cursor_capture(window)` after registration changes so cursor confinement/visibility updates immediately without app-owned grab calls. | `src/renderer/src/api/renderer.rs`, `docs/internal/09-input-winit-integration.md` | Never call `set_cursor_grab(None)` from `CursorLeft` or repeatedly reapply `Confined` per frame. Track the last successful request separately from compositor activation. | `cargo test -p renderer cursor_grab_policy`, renderer/app checks, and a real-Wayland leave/re-enter smoke. | `DECISION-20260707-01` | `renderer-wayland-pointer-constraints.md` |
| API-20260707-02 | App-owned event lifecycle helper | active | `engine::events::runtime_event_bus()` constructs a recorded caller-owned `EventBus`; `RuntimeEventDispatcher` emits/drains staged lifecycle events against a caller-owned bus, including `frame_started`, `drain_input`, and `frame_ended`. | `src/events.rs`, `src/lib.rs`, `src/runtime.rs`, `tests/runtime_event_dispatcher.rs` | Raw `engine_events` primitives remain re-exported and usable directly. `Renderer::events()` and `Renderer::events_mut()` remain compatibility APIs for renderer-owned legacy frame/input paths. | `cargo test -p engine`, `cargo test -p engine_events`, `cargo test -p renderer`, `rg -n "EventBus|FrameStarted|FrameEnded|events_mut\\(|drain_stage|dispatch_pending" src apps tests`. | `DECISION-20260707-02` | none |
| API-20260707-03 | Dogfood app-owned runtime proof | active | `apps/dungeon_dogfood` owns its input system, event bus, frame clock, camera/controller, and audio telemetry while rendering through caller-provided `CameraView` APIs. | `apps/dungeon_dogfood/src/main.rs`, `apps/dungeon_dogfood/src/events.rs`, `apps/dungeon_dogfood/src/audio_bridge.rs`, `docs/api/14-dogfood-vertical-slice.md` | Dogfood must not use renderer-owned input dispatch, `Renderer::events_mut()`, or renderer-owned gameplay camera state on the active path. Renderer still owns Vulkan frame submission, assets, resize, capture output, and platform/UI side effects. | `cargo check -p dungeon_dogfood`, `cargo test -p dungeon_dogfood`, `cargo check`, legacy-call grep, windowed smoke, headless draw capture. | `DECISION-20260707-01`, `DECISION-20260707-02` | none |
| API-20260707-04 | Root engine bin plus lib facade | active | The root `engine` crate provides the data-driven launcher binary and a thin library facade with `camera`, `events`, `frame`, `input`, `render`, and `prelude` modules. | `src/main.rs`, `src/lib.rs`, `src/camera.rs`, `src/events.rs`, `src/frame.rs`, `src/input.rs`, `src/render.rs` | The root facade must not hide raw support crates or become a required runtime object. Apps may use root facade modules or raw crates directly. | `cargo test -p engine`, `tests/facade_imports.rs`, `cargo check`. | `DECISION-20260707-03` | none |
| API-20260707-05 | Renderer-owned camera view DTO | active | `CameraView` lives in the renderer facade because the renderer consumes it at the scene-to-render-submission boundary; the root facade re-exports it for app-owned loops. | `src/renderer/src/api/renderer.rs`, `src/renderer/src/api/mod.rs`, `src/render.rs`, `tests/facade_imports.rs` | Keep the DTO Vulkan-opaque and caller-owned. Do not move renderer internals into root `engine`. | `cargo test -p renderer`, `cargo test -p engine`, `cargo check -p renderer --examples`. | `DECISION-20260707-04` | none |
| API-20260707-06 | Core app-owned loop primitives | active | Root `engine` exposes app-owned workflow helpers at stable module paths: `engine::input::route_platform_input_to_app(renderer, window, input, event) -> Result<RendererInputRouting, RendererError>`; `engine::input::queue_routed_input_event(input, routing, event) -> bool`; `engine::frame::begin_app_frame(input, action_events, events, frame_clock) -> AppFrameBeginReport`; `engine::frame::end_app_frame(events, frame_index) -> AppFrameEndReport`; `engine::frame::{AppFrameBeginReport, AppFrameEndReport, FixedStepClock, FixedStepConfig, FixedStepUpdate}`; `engine::render::camera_view_for_size(camera, width, height) -> CameraView`. | `src/input.rs`, `src/frame.rs`, `src/render.rs`, `src/lib.rs`, `tests/facade_imports.rs`, `apps/dungeon_dogfood/src/main.rs` | Helpers are additive and do not remove raw support-crate primitives, renderer-owned compatibility APIs, renderer examples, or direct `RuntimeEventDispatcher` use. `FixedStepClock` is pure and does not own physics/gameplay. | `cargo test -p engine`, `cargo check -p dungeon_dogfood`, `cargo check -p renderer --examples`, `cargo check`; grep gates for compatibility/stale guidance. | `DECISION-20260707-05` | none |
| API-20260720-01 | Dogfood windowed hotkeys | active | `dungeon_dogfood` binds `F` to `noclip.toggle`, which toggles raw three-axis player movement without wall, floor, or ramp resolution, and binds `C` to `capture.screenshot`, which queues one renderer draw-target capture under a timestamped `captures/` run directory. Trigger, toggle state, capture success, and capture failure are logged. | `apps/dungeon_dogfood/src/main.rs`, `apps/dungeon_dogfood/src/player.rs`, `apps/dungeon_dogfood/src/collision.rs`, `docs/api/14-dogfood-vertical-slice.md` | Hotkeys remain app-owned actions and must pass through the existing app-owned input snapshot/action-event frame boundary. Capture execution remains renderer-owned. | `cargo check -p dungeon_dogfood`, focused noclip tests, and windowed hotkey smoke when practical. | `DECISION-20260707-01` | none |
| API-20260718-01 | Scene frustum-culling facade | active | `Scene::set_frustum_culling(bool)` controls submission culling and `Scene::frustum_culling_enabled()` reports it. New scenes default to enabled. As of Phase 06, mesh-backed nodes carry authoritative `SceneBounds` via `SceneNode.mesh_bounds` and `node_world_bounds`/`subtree_world_bounds`; culling uses `Known(Aabb)` / `Proxy(Aabb)` / `ConservativeVisible(BoundsUnknownReason)` with 8-corner world transforms and post-order subtree aggregation. `Scene::add_mesh` marks bounds `ConservativeVisible(MissingGeometry)`. `Scene::add_mesh_with_bounds`, `set_node_proxy_bounds`, `clear_node_proxy_bounds`, and `AssetManager::mesh_scene_bounds` provide the explicit-bounds API. | `src/renderer/src/api/scene.rs`, `src/renderer/src/scene/scene_world.rs`, `src/renderer/src/data/camera.rs`, `src/renderer/src/api/assets.rs` | Preserve the opt-out path. Plane extraction uses Vulkan `[0, 1]` depth. Known subtrees prune safely; conservative-visible subtrees always traverse. Do not convert an uncomputed known mesh to a unit proxy. | `cargo test -p renderer`, `cargo check -p renderer --examples`, and on/off `capture_culling` draw captures. | `DECISION-20260718-02` | `renderer-frustum-culling-behavior.md` |
| API-20260718-02 | Renderer terminal errors and frame outcomes | active | Vulkan device loss reaches callers as `RendererError::DeviceLost`; a backend that previously panicked or returned a terminal Vulkan error rejects later backend operations with `RendererError::BackendPoisoned`. Transient zero/empty capability states before old-generation retirement return `RendererError::Frame(RendererFrameError::Resize(_))` and remain retryable; compatibility violations and post-retirement failures stay terminal. `FrameRenderOutcome` distinguishes rendered, transient acquire-unavailable, resize-skipped, submitted-not-presented, and presented-suboptimal frames. | `src/renderer/src/api/errors.rs`, `src/renderer/src/api/renderer.rs`, `src/renderer/src/vulkan/vk_render.rs` | Applications must destroy and recreate the renderer after `DeviceLost` or `BackendPoisoned`. Do not collapse acquire/present retry or suboptimal results into `Rendered`; bounded transient acquire exhaustion must not create resize-pending state. | `cargo test -p renderer`, `cargo check -p renderer`, and timeout-bound headless `api_test` smoke. | `DECISION-20260718-03` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` |
| API-20260718-03 | Scene typed-light and directional-shadow facade | active | `create_directional_light` preserves the legacy zero/one route; `add_directional_light` adds direct-light directionals up to the fixed cap of four. Point and spot caps remain 16. `DirectionalShadowConfig` selects at most one directional shadow owner; enabling point/spot shadow intent returns `UnsupportedLightFeature`. All active directional, point, and spot lights flow through `RenderSubmission` and `EnvironmentUBO` into PBR direct lighting. | `src/renderer/src/api/scene.rs`, `src/renderer/src/scene/render_submission.rs`, `src/renderer/src/data/gpu_data.rs`, `src/renderer/src/vulkan/vk_shadow.rs` | The legacy creator still rejects a second light and default builds retain the legacy single-map shadow behavior. Additive directionals never implicitly take shadow ownership. Color is clamped non-negative; finite/range/cone validation and stale slot+generation errors apply before mutation. | `cargo test -p renderer`, feature/all-feature example checks, `capture_shadows` legacy/CSM comparison, and `capture_csm` evidence. | `DECISION-20260718-01`, `DECISION-20260725-03` | `renderer-directional-shadow-behavior.md`, `2026-07-19-renderer-csm-cascade-fitting.md` |
| API-20260725-01 | Mesh geometry query | active | `AssetManager::mesh_geometry(MeshHandle) -> Result<MeshGeometryDto, AssetError>` returns a Vulkan-free snapshot of CPU mesh geometry keyed by `MeshHandle`. `AssetManager::mesh_local_aabb(MeshHandle) -> Result<Option<MeshLocalAabb>, AssetError>` returns the conservative local AABB. Both validate handle generation. | `src/renderer/src/api/assets.rs`, `src/renderer/src/data/mesh_geometry.rs` | Return existing error shapes (InvalidHandle, StaleHandle, NotLoaded, OutOfBounds). Do not expose cache guards or mutable DTO access. Re-export only neutral DTO/value types. | `cargo test -p renderer mesh_geometry -- --nocapture` | `DECISION-20260725-06` | none |
| API-20260725-04 | Scene bounds types and API | active | `SceneBounds::Known(Aabb)` / `Proxy(Aabb)` / `ConservativeVisible(BoundsUnknownReason)` and `MeshBoundsEntry` encode per-mesh culling state. `BoundsUnknownReason` variants: `MissingGeometry`, `StaleHandle`, `Skinned`, `Deformed`, `InvalidGeometry`. `Scene::add_mesh_with_bounds(node, mesh, bounds)` attaches with explicit trust; `Scene::add_mesh(node, mesh)` marks `ConservativeVisible(MissingGeometry)`. `Scene::set_node_proxy_bounds(node, Aabb)` / `clear_node_proxy_bounds(node)` manage explicit stand-in bounds. `AssetManager::mesh_scene_bounds(mesh)` converts DTO to `SceneBounds`. `SceneFragmentNode` carries `mesh_bounds` for merge-safe import. | `src/renderer/src/api/scene.rs`, `src/renderer/src/scene/scene_world.rs`, `src/renderer/src/api/assets.rs`, `src/renderer/src/data/camera.rs` | Proxy bounds must be finite and ordered; a node proxy is accepted only for an empty node or mesh entries explicitly marked `MissingGeometry`. Known, stale, skinned, deformed, and invalid geometry cannot be overridden. Otherwise `ConservativeVisible` dominates. Keep `Scene::add_mesh` compatible but mark bounds conservative-visible. | `cargo test -p renderer scene -- --nocapture`, `cargo test -p renderer culling -- --nocapture` | `DECISION-20260725-12` | `renderer-frustum-culling-behavior.md` |
| API-20260725-03 | Mesh handle fence-aware unload | active | `AssetManager::unload_mesh(MeshHandle) -> Result<(), AssetError>` invalidates the handle immediately (generation bump and geometry lookup removal) while retaining the geometry DTO and GPU payload for deferred destruction. Querying the stale handle returns `StaleHandle`. GPU suballocations are not freed until the fence signals for the `retire_after` serial. Double-unload is idempotently stale. Reserved default slots are rejected. | `src/renderer/src/api/assets.rs`, `src/renderer/src/data/data_cache.rs`, `src/renderer/src/data/retirement.rs` | Preserve `StaleHandle` on query after unload. Do not return the slot to the free list before reaping. Do not silently skip retirement for loaded meshes. | `cargo test -p renderer retirement -- --nocapture`, `cargo test -p renderer`, GPU smoke `mesh_unload` tests. | `DECISION-20260725-15` | none |
| API-20260725-02 | Feature flag compilation gates | active | Cargo features `csm`, `instancing`, and `scene-bvh` compilation-gate new rendering paths. All excluded from `default`. No runtime branches in Phase 01. | `src/renderer/Cargo.toml` | Features must compile independently, together (`--all-features`), and disabled without behavior changes. | `cargo check -p renderer --features csm`, `--features instancing`, `--features scene-bvh`, `--all-features`, default. | `DECISION-20260725-07` | none |
| API-20260725-05 | Renderer retirement serial observation | active | `Renderer::retirement_serials() -> RetirementSerials` exposes latest-successful-submit and fence-observed-completion serials for app-owned generation stores. Renderer API re-exports the generic retirement vocabulary without physics types. | `src/renderer/src/api/renderer.rs`, `src/renderer/src/api/mod.rs` | Completion never advances from submit alone. App stores invalidate immediately and reap only through `latest_completed`. This API must not become a renderer-owned collider bridge. | Renderer checks plus dogfood pre-/post-fence recipe retirement tests. | `DECISION-20260725-15` | `renderer-neutral-geometry-lifetime.md` |
| API-20260725-06 | Dogfood collider validation mode | active | `dungeon_dogfood --validate-colliders` performs deterministic fixed-step collision proof in headless mode and logs recipe identities, body/collider IDs, contact records, and scene transform writeback. | `apps/dungeon_dogfood/src/main.rs` | This is app-local, not a global renderer flag. Missing contact or writeback is a non-zero validation failure. | Timeout-bound headless draw run with retained log and sidecar. | `DECISION-20260725-04` | none |
| API-20260722-01 | Voxel demo CLI and route selection | active | `voxel_demo` accepts separated `--flag value` options. `--preset <default\|cavernous\|mazy\|tight>` and `--config <path>` are mutually exclusive complete-base selectors. Explicitly present generator overrides are `--seed`, `--resolution`, `--shell-thickness`, `--cavern-count`, `--tunnel-count`, `--tunnel-radius-min`, `--tunnel-radius-max`, `--cavern-radius-min`, `--cavern-radius-max`, `--spline-tension`, `--roughness`, `--maze-density`, `--maze-twistiness`, `--maze-radius`, `--maze-retries`, `--maze-search-budget`, `--floor-threshold`, `--wall-uv-scale`, and `--floor-uv-scale`. Runtime options are `--light-budget`, `--headless`, `--capture-dir`/`--capture_dir`, `--env`, and windowed-only `--regen-after-frames`. | `apps/voxel_demo/src/cli.rs`, `apps/voxel_demo/src/main.rs`, `apps/voxel_demo/src/config.rs` | Options are single-occurrence and omission-aware; capture spellings are one logical option. Shared legacy flags alone retain v1. A preset/config, regeneration trigger, or v2-only override selects the strict-document path. Relative config paths prefer an existing regular file under the process working directory, then the voxel-demo package directory, then the workspace root; missing-path errors list every candidate. Unknown flags, equals forms, malformed/missing values, duplicate options, and base conflict fail. Runtime values do not enter saves or identities. | CLI/parser path-resolution tests, `cargo check -p voxel_demo`, documented launch commands. | `DECISION-20260721-01` | `voxel-demo-config-contracts.md`, `voxel-demo-config-regeneration.md` |
| API-20260722-02 | Voxel `PresetDocument` and identities | active | A `PresetDocument` is a complete strict TOML value with schema/generator/RNG versions, one complete `GeneratorSection`, and complete wall/floor `MaterialTheme` values. Supported version tuples are 1/1/1 and 2/2/2. Config resolution normalizes signed zero, resolves catalog/filesystem references from source context, validates checked bounds/capacities/work, and computes domain-separated `GeometryIdentity` and `SceneConfigIdentity`. Canonical save preserves catalog IDs and writes filesystem refs as normalized absolute `non_portable=true` paths. | `apps/voxel_demo/src/config.rs`, `apps/voxel_demo/src/validate.rs`, `apps/voxel_demo/presets/*.toml` | Geometry identity excludes classifier/material/runtime state. Scene-config identity adds classifier, resolved references, material factors, UV scales, and fixed lighting policy. Formatting, raw bytes, source labels, light budget, headless/capture/env/editor state, and content digests are excluded. Asset digests remain separate metadata. | Strict load/save/load tests, identity mutation/exclusion tests, embedded-byte tests, `cargo test -p voxel_demo`. | `DECISION-20260722-01`, `DECISION-20260722-04` | `voxel-demo-config-contracts.md`, `voxel-demo-config-regeneration.md` |
| API-20260722-03 | Voxel editor and regeneration controls | active | Windowed v2 registers the `voxel_editor` imgui callback by default. F1 or F2 toggles callback registration and immediately refreshes renderer-owned cursor capture. The editor is pinned to the viewport's upper-left, starts at 400×600, remains constrained to the viewport, and scrolls overflowing content. It loads/replaces complete drafts, edits generator/material values, validates inline, saves canonical snapshots, randomizes draft seed, and explicitly queues regeneration. Registered app UI suppresses app camera/gameplay routing; hidden UI restores it. Regeneration is latest-wins with one CPU worker and one latest pending request; renderer resources and commit/rollback remain on the event-loop thread. | `apps/voxel_demo/src/editor.rs`, `apps/voxel_demo/src/regeneration.rs`, `apps/voxel_demo/src/main.rs` | Editing/loading/saving/randomizing never implicitly regenerates. Invalid drafts cannot save/regenerate. Registered-panel viewport click-through is not promised. Headless v2 has no editor/regeneration controller. Procedural upload remains synchronous. | Editor queue/lifecycle tests, regeneration stale/rollback tests, `cargo test -p voxel_demo`. | none | `voxel-demo-config-regeneration.md` |
| API-20260722-05 | Generation exhaustion error contract | active | Slot+generation stores that replace `wrapping_add` with `checked_add` return `GenerationExhausted` before collision. `AssetError::GenerationExhausted` and `SceneError::GenerationExhausted` are the public error variants. Stale handle queries after exhaustion return `StaleHandle`. | `src/renderer/src/data/handles.rs`, `src/renderer/src/api/errors.rs`, `src/renderer/src/data/data_cache.rs` | Applies to all slot+generation stores (textures, materials, meshes, scene nodes, lights). The existing `CacheError` enum must gain a `GenerationExhausted` variant. | Handle-exhaustion tests, `cargo test -p renderer`. | `DECISION-20260722-07`, `DECISION-20260725-15` | none |
| API-20260722-06 | Texture and material fence-aware retirement | active | `unload_texture` and `unload_material` invalidate handles immediately but enqueue GPU payloads for deferred destruction via `GpuRetirementQueue` keyed by `RetirementClass::TextureGeometry` / `RetirementClass::MaterialPayload`. Querying a stale handle returns `StaleHandle`. Slots are not returned to the free list until reaping. Double-unload is idempotently stale. | `src/renderer/src/api/assets.rs`, `src/renderer/src/data/retirement.rs`, `src/renderer/src/data/data_cache.rs` | New `RetirementClass` variants required. Does not change the public unload API signature. Does not affect mesh retirement (already compliant). | `cargo test -p renderer retirement`, retirement texture/material tests, multi-frame validation-layer tests. | `DECISION-20260722-07`, `DECISION-20260725-15` | none |
| API-20260722-07 | Hook failure observation contract | active | Safe facade hook failures are aggregated in a `HookReport` per frame with per-hook status and error payload. Registration errors fail directly at `register_hook` with a typed `RendererError`. Backend-mutating extension failures abort through the established drain or poison boundary. A successful submit is never returned as a retryable render failure. | `src/renderer/src/api/hooks.rs`, `src/renderer/src/api/errors.rs`, `src/renderer/src/vulkan/vk_render.rs` | `HookReport` is a new public type. Existing hook trait signatures may change to return `Result`. Frame render outcome API unchanged. | Hook registration/report tests, `cargo test -p renderer`. | `DECISION-20260722-07` | none |
| API-20260722-08 | Renderer public compatibility and animation atomicity | active | `Scene::pick` and `Scene::pick_last_camera` are immutable read-only queries. `AnimationChannel` keeps historical public fields `node_index`, `target_path`, and `sampler_index`. Legacy `AnimationPlayer::set_clip`, `set_speed`, and `update` stay infallible; fallible `try_set_clip`, `try_set_speed`, and `try_update` return `Result`. Invalid animation inputs reject before mutating playback time, play state, speed, or lazy resolved target cache. | `src/renderer/src/api/scene.rs`, `src/renderer/src/scene/scene_world.rs`, `src/renderer/src/animation/mod.rs`, `src/renderer/tests/compatibility.rs`, `src/renderer/tests/integration.rs` | Historical caller syntax must continue compiling. Read-only picking must not refresh dirty/cached world transforms or bounds. Candidate validation must commit animation state only after successful validation and evaluation. | `cargo test -p renderer`, `cargo test -p engine`, `cargo check`. | none | `renderer-animation-atomicity.md` |
| API-20260722-04 | Maintained guide checkpoint | active | `examples/guide_app` is the maintained executable checkpoint for the `docs/guide/` end-user documentation. It is a nested Cargo workspace (NOT in root `Cargo.toml`) that depends on `engine` via `path = "../.."` and `winit = "0.29"`. The checkpoint owns a winit event loop, window, renderer, startup scene, `InputSystem`, `InputActionEventEmitter`, `EventBus`, `FixedStepClock`, `FrameClock`, `Camera`, and `FPSController`. It routes platform input via `engine::input::route_platform_input_to_app`, begins/exactly one app frame per redraw via `begin_app_frame`/`end_app_frame`, builds a `CameraView` via `engine::render::camera_view_for_size`, calls `renderer.render_scene_with_view` (not `render_scene`), handles every `FrameRenderOutcome` variant, and terminates on `DeviceLost`/`BackendPoisoned`. Imports use `engine::prelude::*` and `engine::input`. No direct `renderer` dependency. | `examples/guide_app/Cargo.toml`, `examples/guide_app/Cargo.lock`, `examples/guide_app/src/main.rs`, `examples/guide_app/README.md` | Checkpoint source must match guide prose exactly. Lockfile must be regenerated on dependency changes. No renderer-owned compatibility input or render calls (`update_input`, `render_scene`). Cargo.lock is source-controlled. | `cargo fmt --check --manifest-path examples/guide_app/Cargo.toml`, `cargo check --locked --manifest-path examples/guide_app/Cargo.toml`, and timeout-bound WSI smoke when available. | `DECISION-20260722-05`, `DECISION-20260722-06` | none |
| API-20260722-09 | Scene v2 persistence format | active | The canonical scene persistence DTO (`format_version = 2`) preserves directional lights, spot lights, collision metadata, audio references, visibility, prefab metadata, and shadow-owner configuration. Save is failure-atomic via a randomly named staged file (write → fsync → atomic rename). Load validates unique stable IDs, parent acyclicity, finite transforms, valid asset references, and ranged light parameters. Three light kinds round-trip under `lights` (point), `directional_lights` (directional), and `spot_lights` (spot). | `src/renderer/src/api/scene.rs`, `src/renderer/src/api/scene_file_tx.rs`, `src/renderer/tests/fixtures/scenes/` | Runtime handles (`SceneNodeId`, `MeshHandle`, `EnvironmentHandle`) and derived state (world transforms, bounds caches, GPU payloads) are excluded from serialization and reconstructed at load. v1 format loads through forward-compatible migration. | `cargo test -p renderer`, scene round-trip tests for all three light kinds, v1/v2 fixture tests. | `DECISION-20260722-07` | none |
| API-20260722-10 | Input multi-binding instance tracking | active | `BindingInstanceId` tracks per-binding contributions to shared actions. Pressing any mapped binding activates; releasing one binding leaves the action active while another instance still contributes. Aggregate action value is max across active instances. `just_pressed`/`just_released` edges fire correctly when the last binding activates or deactivates. `InputContext::set_instance_value` enables caller-assigned multi-source aggregation; `set_action_value` (single anonymous instance) remains for simple use. | `src/input/src/lib.rs` | Modifier-release ordering is per-binding, not global. Each binding tracks its own trigger/modifier state though `BindingInstanceId`. | `cargo test -p input`, multi-binding action tests, modifier-ordering tests. | `DECISION-20260707-01` | none |
| API-20260722-11 | Fail-closed staging and symlink policy | active | `engine_pack` mutating commands use transactional staging: preflight validation, sibling staging directory, atomic rename to target. Existing targets are rejected (`ExistingTarget` error). `scan-assets` and `pack` use `symlink_metadata` (never follows symlinks), per-entry canonicalization with root containment, and visited-set cycle detection. `new-app`, `new-project`, and `pack` refuse existing target directories. `add-asset` uses backup/rename for manifest mutation with rollback on validation failure. | `tools/engine_pack/src/fs_tx.rs`, `tools/engine_pack/src/main.rs` | Symlinks are silently skipped, not followed. Non-UTF-8 paths are rejected at the scan boundary. All asset-registry output is serializer-backed (TOML/JSON via `serde`); ad-hoc escape/ID sanitization is removed. | `cargo test -p engine_pack`, symlink-escape tests, cycle-detection tests, staging-failure tests. | `DECISION-20260722-07` | none |

## Runtime Launch Registry

Keep this registry current with runtime launch surfaces. Use paths relative to the repository root.

### Shared Runtime Flags

| scope | flags | notes |
| --- | --- | --- |
| Root `engine` launcher | `--project <path>` or `--project=<path>` | Required. Points at an `engine.project.toml`. |
| Root `engine` launcher | `--scene <path>` or `--scene=<path>` | Optional scene override. If absent, uses the project's `startup_scene`. |
| Root `engine` launcher | `--headless` | Uses the headless runtime path. |
| Root `engine` launcher | `-h`, `--help` | Prints usage. Cannot be combined with runtime options. |
| Root `engine` and renderer examples | `--record_debug <seconds>`, `--record_debug=<seconds>` | Starts debug timing capture. Value must be >= 1. |
| Root `engine` and renderer examples | `--record_debug_interval <ms>`, `--record_debug_interval=<ms>` | Debug timing sample interval. Value must be >= 1. |
| Root `engine` and renderer examples | `--record_debug_path <path>`, `--record_debug_path=<path>` | Writes debug timing JSONL to an explicit path. |
| Root `engine` and renderer examples | `--capture_target <present\|draw>`, `--capture_target=<present\|draw>` | `present` is default. Use `draw` for headless draw-target validation. |
| Root `engine` and renderer examples | `--capture_frame <n>`, `--capture_frame=<n>` | Single frame capture. Root/examples require positive `n`. |
| Root `engine` and renderer examples | `--capture_frame_path <path>`, `--capture_frame_path=<path>` | Requires `--capture_frame`. |
| Root `engine` and renderer examples | `--capture_frames <n>`, `--capture_frames=<n>` | Sequence capture count. Requires `n >= 1`. |
| Dogfood | `--validate-colliders` | Runs deterministic app-owned mesh-collider contact and transform-writeback validation; intended for headless validation. |
| Dogfood | `--capture_frames <n>` | Sequence capture count. Current dogfood parser does not accept equals form; see local bug `dogfood-capture-equals-flags-documented-but-not-parsed`. |
| Root `engine` and renderer examples | `--capture_frame_start <n>`, `--capture_frame_start=<n>` | Requires `--capture_frames`. Root/examples require positive `n`. |
| Dogfood | `--capture_frame_start <n>` | Requires `--capture_frames`. Current dogfood parser accepts non-negative `n` and does not accept equals form. |
| Root `engine` and renderer examples | `--capture_frame_interval <n>`, `--capture_frame_interval=<n>` | Requires `--capture_frames`; `n >= 1`. |
| Dogfood | `--capture_frame_interval <n>` | Requires `--capture_frames`; `n >= 1`. Current dogfood parser does not accept equals form. |
| Root `engine` and renderer examples | `--capture_dir <dir>`, `--capture_dir=<dir>` | Requires `--capture_frames`. |
| Dogfood | `--capture_dir <dir>` | Requires `--capture_frames`. Current dogfood parser does not accept equals form. |
| Root `engine` and renderer examples | `--manual_capture_dir <dir>`, `--manual_capture_dir=<dir>` | Directory used for manual capture requests. |

### Application Entrypoints

| surface | command | supported arguments | current project/scene defaults | notes |
| --- | --- | --- | --- | --- |
| Root runtime launcher | `cargo run -- --project apps/dungeon_dogfood/engine.project.toml` | Shared root launcher flags above. | Project startup scene is `apps/dungeon_dogfood/scenes/start.engine.scene.json`. | Uses `src/main.rs` and `src/runtime.rs`; best generic project-manifest launch path. |
| Dungeon dogfood app | `cargo run -p dungeon_dogfood` | `--level <selector-or-path>`, `--headless`, `--capture_target`, `--capture_frames`, `--capture_frame_start`, `--capture_frame_interval`, `--capture_dir`, `--validate-colliders`. Environment variables: `DUNGEON_DOGFOOD_LEVEL`, `DUNGEON_DOGFOOD_GENERATOR_SEED`, `DUNGEON_DOGFOOD_GENERATOR_WIDTH`, `DUNGEON_DOGFOOD_GENERATOR_HEIGHT`, `DUNGEON_DOGFOOD_GENERATOR_LAYERS`, `DUNGEON_DOGFOOD_VALIDATION`. | Built-in selectors: generated default, `level_01`, `level_02_ramps`, `level_03_lighting`; project manifest exists at `apps/dungeon_dogfood/engine.project.toml`. | Runtime app parses its own level/capture args rather than the shared root launcher. |
| Voxel demo app | `cargo run -p voxel_demo -- --preset default` | Base: `--preset <default\|cavernous\|mazy\|tight>` or `--config <path>`. Overrides: `--seed`, `--resolution`, `--shell-thickness`, `--cavern-count`, `--tunnel-count`, `--tunnel-radius-min`, `--tunnel-radius-max`, `--cavern-radius-min`, `--cavern-radius-max`, `--spline-tension`, `--roughness`, `--maze-density`, `--maze-twistiness`, `--maze-radius`, `--maze-retries`, `--maze-search-budget`, `--floor-threshold`, `--wall-uv-scale`, `--floor-uv-scale`. Runtime: `--light-budget`, `--headless`, `--capture-dir`/`--capture_dir`, `--env`, `--regen-after-frames`. | No v2 selector preserves legacy v1. The v2 default base is the embedded `default` document. Headless v2 captures five core-role viewpoints and writes renderer plus enriched sidecars; windowed v2 starts with the imgui editor registered. | App-local options accept only separated value syntax. `--regen-after-frames` is windowed validation/debug behavior. See `docs/api/16-voxel-demo.md`. |
| Engine pack tool | `cargo run -p engine_pack -- <command>` | `validate-package`, `validate-project`, `validate-scene`, `new-app`, `new-project`, `new-package`, `scan-assets`, `add-asset`, `pack`. | Uses explicit paths per command. | Packaging/validation CLI, not a renderer runtime. |
| Guide checkpoint app | `cargo run --manifest-path examples/guide_app/Cargo.toml` | Standard winit WSI. No custom CLI flags. | Uses the renderer startup scene via `renderer.take_startup_scene()`. Working directory must be the repository root (`vulkan-engine/`). | Nested workspace; NOT part of root `Cargo.toml`. See `docs/guide/04-app-owned-loop.md`. |

### Renderer Example Entrypoints

| surface | command | supported arguments | notes |
| --- | --- | --- | --- |
| PBR demo | `cargo run -p renderer --example demo_pbr` | Renderer example flags: `--env`, `--model`, debug flags, capture flags, `--headless`, manual capture dir. | General PBR/material smoke path. |
| Unlit demo | `cargo run -p renderer --example demo_unlit` | Renderer example flags. | Unlit shader/runtime smoke path. |
| Model-load demo | `cargo run -p renderer --example demo_model_load` | Renderer example flags. | glTF/model loading smoke path. |
| Async loading demo | `cargo run -p renderer --example demo_async_loading` | Renderer example flags. | Asset task and async loading smoke path. |
| API test demo | `cargo run -p renderer --example api_test` | Renderer example flags. `--env src/renderer/src/assets/sky_maps/indoor_4k.exr` is a useful environment override. | Renderer facade and environment runtime smoke path. |
| Capture geometry | `cargo run -p renderer --example capture_geometry -- --headless` | Capture-test flags: `--headless`, `--capture_target`, `--capture_frames`, `--capture_frame_start`, `--capture_frame_interval`, `--capture_dir`. | Baseline visual capture scene. |
| Capture culling | `cargo run -p renderer --example capture_culling -- --headless --culling=on` | Capture-test flags plus `--culling=on` or `--culling=off`. | Deterministic on/off visual-parity scene with off-screen draws and an in-frustum child beneath an off-screen parent. |
| Capture lighting | `cargo run -p renderer --example capture_lighting -- --headless` | Capture-test flags. | Baseline point-light visual capture scene. |
| Capture shadows | `cargo run -p renderer --example capture_shadows -- --headless` | Capture-test flags. | Deterministic directional-light scene with two opaque casters and a receiver plane. |
| Capture transform | `cargo run -p renderer --example capture_transform -- --headless` | Capture-test flags. | Baseline visual capture scene; current validated baseline has a framing caveat. |
| Capture material PBR | `cargo run -p renderer --example capture_material_pbr -- --headless` | Capture-test flags. | Baseline visual capture scene. |
| Capture environment | `cargo run -p renderer --example capture_environment -- --headless` | Capture-test flags. | Baseline visual capture scene. |
| Capture model load | `cargo run -p renderer --example capture_model_load -- --headless` | Capture-test flags. | Baseline visual capture scene. |

## Drift Records

| id | spec | status | observed_drift | impact | routing | source | review_after |
| --- | --- | --- | --- | --- | --- | --- | --- |
