---
schema_version: 1
document_type: architecture-specification
status: active
owner: architecture
created: 2026-07-03
---

# Architecture Specification

## Active Contracts

| id | area | status | intended_contract | observed_anchors | drift_gaps | validation | related_decisions | related_knowledge |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ARCH-20260703-01 | Workspace layout | active | The root `engine` package provides a data-driven launcher binary and a thin app-facing library facade; renderer examples remain supported diagnostics and compatibility references. Separate support crates and apps remain independent workspace members. | `Cargo.toml`, `src/main.rs`, `src/lib.rs`, `src/launch.rs`, `src/runtime.rs`, root `AGENTS.md` | None currently recorded. | `cargo check`, crate-specific checks, and example checks for touched areas. | `DECISION-20260707-03`, `ARCH-20260707-04` | none |
| ARCH-20260703-02 | Renderer visual evidence | active | Renderer, scene, shader, camera, material, asset, and Vulkan behavior that claims visual proof uses headless capture evidence instead of desktop screenshots by default. | `.internal-dev/skills/engine-headless-capture-validation/SKILL.md` | None currently recorded. | Timeout-bound headless captures with PNG and JSON sidecars. | none | none |
| ARCH-20260707-01 | App-owned input routing | active | Root/app runtime paths may own `InputSystem` dispatch and action-event emission while the renderer owns platform/UI/debug/capture side effects and returns an input routing decision. Legacy renderer-owned input remains compatibility behavior. | `src/input.rs`, `src/renderer/src/api/renderer.rs`, `apps/dungeon_dogfood/src/main.rs`, `docs/internal/09-input-winit-integration.md` | Renderer examples still use the legacy compatibility path by design. | `cargo test -p engine`, `cargo test -p renderer`, `cargo check -p dungeon_dogfood`, legacy-call grep for migrated apps. | `DECISION-20260707-01` | none |
| ARCH-20260707-02 | App-owned event lifecycle | active | Root/app runtime paths may own one `EventBus` for lifecycle, input, and subsystem events. `RuntimeEventDispatcher` emits/drains lifecycle stages against a caller-owned bus; renderer-owned lifecycle events remain compatibility behavior for legacy renderer frame APIs. | `src/events.rs`, `src/runtime.rs`, `tests/runtime_event_dispatcher.rs`, `apps/dungeon_dogfood/src/main.rs`, `docs/api/12-events-and-lifecycle.md` | Renderer legacy frame APIs still emit renderer-owned lifecycle events for compatibility. | `cargo test -p engine`, `cargo test -p engine_events`, `cargo test -p renderer`, grep for `FrameStarted`/`FrameEnded`. | `DECISION-20260707-02` | none |
| ARCH-20260707-03 | Dogfood runtime ownership proof | active | `dungeon_dogfood` is the real-app proof that input, lifecycle events, camera/player update, and audio telemetry can be app-owned while renderer performs render/capture work from supplied scene/view data. | `apps/dungeon_dogfood/src/main.rs`, `apps/dungeon_dogfood/src/audio_bridge.rs`, `apps/dungeon_dogfood/src/events.rs`, `.internal-dev/captures/engine-runtime-abstractions-issues-35-37/phase-05-dogfood/` | Windowed smoke currently shows swapchain acquire retry warnings in this environment; headless draw capture succeeds. | `cargo check -p dungeon_dogfood`, `cargo test -p dungeon_dogfood`, `cargo check`, windowed smoke, headless draw capture. | `DECISION-20260707-01`, `DECISION-20260707-02` | none |
| ARCH-20260707-04 | Root launcher and app facade crate shape | active | The root `engine` crate intentionally has both a binary launcher and library facade. The binary remains the data-driven project launcher; the library remains a thin app-facing reexport/helper layer over support crates. | `Cargo.toml`, `src/main.rs`, `src/lib.rs`, `src/runtime.rs`, `src/launch.rs` | Root facade must not become a monolithic engine runtime or introduce reverse dependencies from support crates. | `cargo check`, `cargo test -p engine`, facade import tests. | `DECISION-20260707-03` | none |
| ARCH-20260707-05 | Render view boundary | active | App-owned camera state crosses into renderer through `CameraView`, a renderer facade DTO re-exported by root `engine::render` and `engine::prelude`. | `src/renderer/src/api/renderer.rs`, `src/render.rs`, `apps/dungeon_dogfood/src/main.rs`, `docs/internal/04-api-to-backend-handoff.md` | `CameraView` must stay Vulkan-opaque and should not expand into a broad runtime abstraction. | `cargo test -p renderer`, `cargo test -p engine`, dogfood headless capture. | `DECISION-20260707-04` | none |
| ARCH-20260707-06 | App-owned workflow primitives layer | active | The root `engine` facade may provide small app-owned workflow primitives in `engine::input`, `engine::frame`, and `engine::render` for input routing, frame lifecycle, fixed-step timing, and camera-view construction. This layer coordinates caller-owned state but does not become a runtime owner. | `src/input.rs`, `src/frame.rs`, `src/render.rs`, `src/lib.rs`, `docs/api/15-app-owned-loop.md`, `apps/dungeon_dogfood/src/main.rs` | Renderer-owned compatibility APIs (`Renderer::update_input`, `render_scene`, `begin_frame`/`end_frame`) and renderer examples remain valid; issue #39 deprecation/removal work is queued only. | `cargo test -p engine`, `cargo check -p dungeon_dogfood`, `cargo check -p renderer --examples`, `cargo check`, docs/spec grep gates. | `DECISION-20260707-05` | none |
| ARCH-20260718-01 | Directional shadow path | active | Without `csm`, the rendergraph records one 2048² D32 directional shadow map per frame slot. With `csm` compiled and a shadow-casting directional enabled, three 1024² D32 cascade layers replace the legacy map. Opaque mesh bounds determine conservative light-space orthographic volumes per cascade; the result is sampled from scene set 0 binding 5 (`sampler2DArrayShadow`) with 3×3 comparison PCF in the PBR shader. Cascade matrices, splits, count, and blend fraction flow through `EnvironmentUBO`. | `src/renderer/src/rendergraph/passes/shadow_pass.rs`, `src/renderer/src/vulkan/vk_shadow.rs`, `src/renderer/src/shaders/material_pbr.frag`, `src/renderer/src/data/gpu_data.rs`, `src/renderer/src/vulkan/vk_commands.rs` | Shadow images are frame-local. CSM feature builds always sample the CSM array view; the legacy map is unused when `csm` is compiled. Point and spot shadow rendering returns `UnsupportedLightFeature`. | `cargo test -p renderer`, `cargo check -p renderer --features csm --examples`, validation-layer headless smoke, `capture_shadows` and `capture_csm` evidence. | `DECISION-20260718-01`, `DECISION-20260725-03` | `renderer-directional-shadow-behavior.md`, `renderer-csm-cascade-fitting.md` |
| ARCH-20260718-02 | Frame transaction and terminal-error boundary | active | After a frame slot is acquired and its fence reset, every path either submits work that signals the fence or records/submits a drain transaction that also retires acquired swapchain synchronization. Device loss is surfaced as a typed terminal error; backend panics and terminal failures poison later backend operations. | `src/renderer/src/vulkan/vk_render.rs`, `src/renderer/src/api/errors.rs`, `src/renderer/src/api/renderer.rs` | New post-acquire early returns must preserve fence, semaphore, and acquired-image retirement. Applications recreate the renderer after `DeviceLost` or `BackendPoisoned`; transparent recovery is not part of this contract. | Frame-transaction failure-injection tests, renderer tests, validation-enabled GPU smoke, and canonical headless smokes. | `DECISION-20260718-03` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` |
| ARCH-20260718-03 | Material draw lifetime boundary | active | Render-bucket resolution copies the Vulkan draw fields needed from `VkLoadedMaterial` into `CopiedMaterialDrawRecord` while the texture-cache lock is held. `RenderObject` contains that by-value record and never retains a raw material pointer across cache mutation or lock release. | `src/renderer/src/data/gpu_data.rs`, `src/renderer/src/vulkan/vk_render.rs`, `src/renderer/src/data/data_cache.rs` | Any new material draw field must be copied under the same cache guard. Do not reintroduce cache-address or raw-pointer coupling into frame draw objects. | `copied_material_draw_record_survives_cache_mutation`, renderer tests, and zero-match raw-pointer grep. | `DECISION-20260718-04` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` |
| ARCH-20260725-01 | Command-recording extraction seam | active | `vk_frame.rs` owns frame transaction/acquire/drain/submit/present mechanics and `vk_commands.rs` owns per-frame command recording and draw-list policy. `VkRenderCore` remains the unique cross-subsystem owner/coordinator. Default passes receive lifetime-bound pass-specific contexts with no unrestricted core access. The only raw-pointer boundary targets the current `VkFrame`; the dispatcher cannot reach presentation state. Public `Renderer` and `CameraView` behavior is unchanged. | `src/renderer/src/vulkan/vk_render.rs`, `src/renderer/src/vulkan/vk_frame.rs`, `src/renderer/src/vulkan/vk_commands.rs`, `src/renderer/src/rendergraph/` | No broad recording trait or generalized pass API. Preserve the confined frame-pointer safety proof when changing dispatcher fields. | `cargo test -p renderer`, canonical example checks/smokes, visual-regression baselines. | `DECISION-20260725-02` | none |
| ARCH-20260725-02 | Neutral mesh geometry DTO | active | Renderer exports a Vulkan-free `MeshGeometryDto` at asset ingestion — before GPU upload — keyed by `MeshHandle`. It contains model-space positions, triangle indices, deformation classification, and a conservative local AABB. Renderer code never depends on physics or the app bridge. | `src/renderer/src/data/mesh_geometry.rs`, `src/renderer/src/api/assets.rs` | DTO contains no Vulkan, Rapier, app, or root-engine types. Keyed by the existing `MeshHandle`; generation is validated on every query. DTO registration is transactional with mesh-handle allocation. | `cargo test -p renderer mesh_geometry -- --nocapture`, sync/async/unload/cancel/rollback proofs. | `DECISION-20260725-06` | none |
| ARCH-20260725-03 | Descriptor ABI manifest | active | Descriptor set 0 (scene), set 1 (skin), set 2 (material samplers), set 3 (material properties), push constants, and pipeline layouts are governed by a documented ABI manifest. Rust layouts, Vulkan descriptor layouts, GLSL declarations, generated SPIR-V, and draw binding order change as one compatibility unit. | `docs/internal/14-renderer-descriptor-abi.md`, `src/renderer/src/vulkan/vk_descriptor.rs`, `src/renderer/src/shaders/` | No descriptor-set layout change without an updated ABI manifest. Scene set 0 binding 5 is the current `sampler2DArrayShadow` compatibility point; the default path binds a one-layer array view and CSM binds three layers. | ABI manifest tests, `cargo check -p renderer`, validation-layer smoke. | `DECISION-20260725-08` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` |
| ARCH-20260725-04 | Feature flags for new rendering paths | active | CSM, instancing, and scene-BVH/new-culling code is compilation-gated behind Cargo features `csm`, `instancing`, and `scene-bvh`. All three are excluded from `default`. When disabled, the default binary is behavior-identical to engine-alpha. | `src/renderer/Cargo.toml` | Features are additive and non-breaking. CSM additionally requires explicit directional shadow configuration in feature builds; default builds preserve legacy implicit single-light shadows. Remove a flag only after its complete evidence branch passes. | `cargo check -p renderer --features csm`, `--features instancing`, `--features scene-bvh`, `--all-features`, and default. | `DECISION-20260725-07` | none |
| ARCH-20260725-05 | Conservative bounds policy | active | Known rigid meshes produce exact local AABBs from CPU positions. Unknown, missing, skinned, and deformed bounds are conservatively visible. Explicit proxy bounds are permitted only when geometry is known-unavailable. A unit proxy is not an acceptable substitute for an uncomputed known mesh AABB. World AABBs use 8-corner transforms; local AABBs derive from DTO `MeshGeometryDto`. Scene nodes own `mesh_bounds: Vec<MeshBoundsEntry>`, `node_world_bounds`, and `subtree_world_bounds`; `SceneBounds::Known(Aabb)` / `Proxy(Aabb)` / `ConservativeVisible(BoundsUnknownReason)` encode culling state. | `src/renderer/src/data/mesh_geometry.rs`, `src/renderer/src/scene/scene_world.rs`, `src/renderer/src/api/scene.rs`, `src/renderer/src/data/camera.rs`, `src/renderer/src/data/assimp_util.rs` | Procedural bounds flow from DTO conversion; Assimp bounds and deformation classifications are captured from CPU import data before GPU promotion and preserved through fragment conversion. Subtree bounds are post-order child unions. Conservative-visible nodes prevent subtree pruning. Exact-generation bounds references are marked with the prospective submit serial, and DTO bounds metadata retires through `RetirementClass::BoundsEntry`. | Bounds propagation tests, culling tests, `cargo test -p renderer scene -- --nocapture`, `cargo test -p renderer culling -- --nocapture`, capture_culling on/off headless evidence. | `DECISION-20260725-12` | `renderer-frustum-culling-behavior.md` |
| ARCH-20260725-06 | Validation matrix separation | active | Deterministic rendering evidence (headless captures) and real-WSI evidence (windowed resize, surface loss, present errors) are recorded in separate phase-tagged matrices. Headless results must never be claimed as WSI evidence. | `.internal-dev/captures/engine-integration-sprint/phase-01/`, `docs/internal/13-engine-integration-contracts.md` | Unavailable WSI entries are marked unexecuted with environment evidence. | Phase-tagged validation criteria. | `DECISION-20260725-09` | none |
| ARCH-20260725-07 | Importer assessment only | active | Importer assessment inventories every format required by repository assets and recommends pure-Rust glTF as the primary path while retaining Assimp for evidenced formats. No dependency changes are made as part of the assessment. | `.internal-dev/reviews/engine-integration-importer-assessment.md` | russimp-sys is retained; `gltf` crate remains the current glTF path. Do not change importer ownership based only on the assessment. | `cargo check -p renderer` confirms no dependency changes. | `DECISION-20260725-10` | none |
| ARCH-20260725-08 | Descriptor reset token invariant | active | Descriptor pool reset must be authorized by a single-use `CompletedFrameSlot` token created by the frame-fence wait path. Before any Vulkan call, the allocator validates slot identity, exact equality with the expected frame serial, and epoch monotonicity. Tokens are consumed on first use; duplicate, mismatched, or stale tokens produce `ResetRejected`. A partial reset failure quarantines every tracked pool as exhausted until a later authorized reset. | `src/renderer/src/vulkan/vk_types.rs`, `src/renderer/src/vulkan/vk_descriptor.rs`, `src/renderer/src/vulkan/vk_render.rs` | Never reset a descriptor pool using a bare boolean or unchecked guard. The token must flow from `wait_for_frame_fence` through `cleanup_curr_frame_resources` to `clear_pools`; no pool from a partially failed reset may remain ready. | `cargo test -p renderer vk_descriptor -- --nocapture`, fault-injection token-rejection and partial-reset tests. | `DECISION-20260725-13` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` |
| ARCH-20260725-09 | Swapchain lifecycle state machine | active | Swapchain replacement is modeled as an explicit Nascent/Current/Retired/Absent state machine in `vk_swapchain.rs`. Once `vkCreateSwapchainKHR` is invoked with non-null `oldSwapchain`, that generation is permanently Retired. Capabilities are re-queried before any state mutation. Resize requests coalesce to the latest event; requested or capability-selected zero extents and transient empty capability sets remain pending without invoking Vulkan or retiring the current generation, and same-extent window notifications do not rebuild a current swapchain. Acquire and present results are classified structurally via `AcquireClass`/`PresentClass` without string parsing. Bounded `NOT_READY`/`TIMEOUT` exhaustion is a transient frame skip, not a resize request; only structural out-of-date/suboptimal results request rebuild. Present image views are destroyed before their owning swapchain handle. | `src/renderer/src/vulkan/vk_swapchain.rs`, `src/renderer/src/vulkan/vk_init.rs`, `src/renderer/src/vulkan/vk_render.rs` | Never restore a Retired generation as current, never pass it again as `oldSwapchain`. Do not call `device_wait_idle` as a substitute for ownership modeling. Never classify Vulkan results through message parsing. | `cargo test -p renderer vk_swapchain -- --nocapture`, `cargo test -p renderer frame_transaction -- --nocapture`, `cargo test -p renderer`, `cargo check -p renderer --examples`, `cargo check` | `DECISION-20260725-09` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` |
| ARCH-20260725-10 | Fence-aware handle retirement | active | Slot+generation handles invalidate immediately but delay payload destruction and slot reuse until the last referencing GPU frame completes. A global `FrameSerial` is assigned at each successful submit and advanced only by fence observations. `GpuRetirementQueue<T>` stores `RetirementRecord<T>` keyed by `retire_after`; `reap_through` destroys payloads and releases slots once `completed_serial >= retire_after`. Reserved default slots are non-retirable. Generation wrap uses `checked_add` and returns an error before collision. Mesh unload goes through `MeshCache::retire_mesh` → `GpuRetirementQueue` → `reap_mesh_retirement`. | `src/renderer/src/data/retirement.rs`, `src/renderer/src/data/data_cache.rs`, `src/renderer/src/vulkan/vk_render.rs`, `src/renderer/src/vulkan/vk_types.rs`, `src/renderer/src/api/assets.rs` | Never free a payload or reuse a slot before the fence for its `retire_after` signals. Never reuse a slot whose retired generation could collide. Completion must advance only from successful fence observations. | `cargo test -p renderer retirement -- --nocapture`, `cargo test -p renderer mesh_geometry -- --nocapture`, `cargo test -p renderer`, `cargo check -p renderer --examples`, `cargo check` | `DECISION-20260725-15` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` |
| ARCH-20260725-11 | Physics convex hull validation | active | `ColliderShape::ConvexHull { points: Vec<[f32; 3]> }` is renderer-independent, valid on static/dynamic/kinematic bodies, and backed by Rapier/Parry without public Rapier type exposure. Input is validated transactionally before collider-set insertion: non-empty, every component finite, deterministically deduplicated, ≥4 unique points, and a rotation-independent, scale-aware affine-basis degeneracy check (line/plane distance relative to maximum model-space extent, epsilon `1e-6`). Model-space coordinates are preserved in the public payload; the app bridge owns instance transforms. | `src/physics/src/lib.rs` | Never expose Rapier types publicly. Never internally fall back to cuboid/sphere/trimesh. Construction failure must leave no side effects. Only `TriMeshStatic` is restricted to static bodies; `ConvexHull` works on all three body kinds. | `cargo test -p physics convex_hull -- --nocapture`, `cargo test -p physics`, `cargo check -p physics`, `cargo check` | `DECISION-20260725-04` | none |
| ARCH-20260725-12 | App-owned mesh collider recipes | active | `dungeon_dogfood` explicitly maps renderer-neutral `MeshGeometryDto` generations to `StaticTrimesh`, `ConvexHull`, or `None`. The bridge remains app-local; renderer has no physics dependency. Geometry remains model-space, scale is baked into physics-owned copies, pose is explicit, and shear/non-finite/non-invertible transforms are rejected. Recipe handles invalidate immediately and queue payloads as `RetirementClass::ColliderRecipe`; slot reuse waits for the fence-derived completed serial. Cancellation/failure/unload remove recipe instances and body-node mappings. | `apps/dungeon_dogfood/src/mesh_collider_bridge.rs`, `apps/dungeon_dogfood/src/scene_seed.rs`, `src/renderer/src/api/renderer.rs`, `src/physics/src/lib.rs` | Keep the bridge app-local until a second consumer exists. No automatic scene walking, convex decomposition, collider LOD, or compound shapes. | Physics/app lifecycle tests, dependency grep/tree, deterministic dogfood collider validation, and workspace checks. | `DECISION-20260725-04`, `DECISION-20260725-06`, `DECISION-20260725-15` | `renderer-neutral-geometry-lifetime.md` |
| ARCH-20260719-01 | Dungeon prefab catalog | active | `dungeon_dogfood` loads strict version-1 TOML prefabs from a canonical, symlink-free catalog; validates tiles, sockets, reservations, markers, ramp transitions, and globally unique component IDs; emits fully revalidated quarter-turn variants; and computes catalog identity as SHA-256 over the domain, version, file count, and canonical relative path/raw-byte frames. | `apps/dungeon_dogfood/src/generator/prefab.rs`, `apps/dungeon_dogfood/assets/prefabs/` | Prefab loading remains app-owned during generator staging. Errors expose stable relative catalog contexts, never absolute host paths. Every complete ramp pattern has exactly one validated transition. | `cargo test -p dungeon_dogfood`, `cargo check -p dungeon_dogfood`, canonical hash golden, malformed-content and bundled-asset tests. | none | none |
| ARCH-20260719-02 | Dungeon Phase 03 structural data flow | active | Phase 03 places explicit lower/upper transition endpoint regions before ordinary rooms, marks hub projections as `TransitionHub`, protects only owner-filtered ramp/opening/landing/headroom masks as transition volume, then builds a separate occupancy-backed `CandidateGraph`. Each reservation owns one concrete adjacent-layer ramp-socket edge, and topology selection proves graph bounds and transition removal against those actual edge IDs. | `apps/dungeon_dogfood/src/generator/ir.rs`, `apps/dungeon_dogfood/src/generator/placement.rs`, `apps/dungeon_dogfood/src/generator/topology.rs` | Structural intent remains app-private and is not accepted/lowered output. Corridor crossings are deterministic, rerouted first, counted, and bounded by normalized configuration. | `cargo test -p dungeon_dogfood`, focused real-entrypoint placement/build/select and transition-removal tests, `cargo check -p dungeon_dogfood`. | `DECISION-20260719-01` | `dungeon-generator-phase-validation.md` |
| ARCH-20260720-01 | Dungeon corridor enclosure materialization | active | After rooms, corridors, ramps, and perimeter walls are materialized, every interior `Void` cell orthogonally adjacent to `Floor` or a ramp becomes `Wall`, except transition-owned vertical openings and shaft cells above lower `Void`/ramp space. | `apps/dungeon_dogfood/src/generator/routing.rs`, `apps/dungeon_dogfood/src/geometry.rs` | Border cells remain governed by perimeter sealing. Transition openings, crest exits, and inferred landing headroom must remain `Void` so ramp inference and traversal stay valid. | Focused corridor-wall tests, seed-77 qualification, `cargo check -p dungeon_dogfood`, package tests, and seed-77 headless draw capture. | `DECISION-20260719-01` | `dungeon-generator-phase-validation.md` |
| ARCH-20260720-02 | Dungeon volumetric voxel geometry | active | Dungeon tile geometry is emitted as closed volumetric primitives: floor slabs (0.10-thick boxes), wall voxels (full-height boxes with 6-face culling against same-layer neighbors, layers above/below, and adjacent ramps), ramp wedges (sloped 3D volumes with clipped side faces integrated against adjacent wall and ramp volumes), and ceiling closures (0.10-thick boxes exposed only when no upper-layer geometry occludes them). A single `emit_tile_geometry` traversal handles both floor and wall domains in one pass, producing a shared material-independent geometry plan keyed by chunk leaf. | `apps/dungeon_dogfood/src/geometry.rs` | Ramp inference suppresses ceiling quads over complete shared ramp runs. Ceiling closures are suppressed by the `upper_layer_covers()` check for upper-layer Floor/Wall coverage. Wall faces adjacent to ramp edges are clipped above the ramp surface rather than emitting full-height quads. | `cargo test -p dungeon_dogfood geometry`, `cargo check -p dungeon_dogfood`. | `DECISION-20260720-01` | `dungeon-generator-phase-validation.md` |
| ARCH-20260721-01 | Voxel demo application | active | `apps/voxel_demo` is a permanent workspace app with explicit v1 compatibility and configurable v2 routes. Shared legacy-only invocations preserve `NormalizedConfig`/PCG32 v1 and immutable goldens. A v2 selector resolves one strict complete `PresetDocument`, applies only explicitly present CLI overrides, validates checked bounds/work, and computes typed identities before topology-first generation. Both routes remain app-owned for camera, input, capture, and event-loop behavior. | `apps/voxel_demo/Cargo.toml`, `apps/voxel_demo/src/main.rs`, `apps/voxel_demo/src/cli.rs`, `apps/voxel_demo/src/config.rs`, `apps/voxel_demo/src/validate.rs` | Supported document tuples are 1/1/1 and 2/2/2. Resolution is 64/96/128. Runtime-only state does not enter v2 saves or identities. No physics, audio, scripting, renderer, shader, or descriptor dependency expansion. | `cargo test -p voxel_demo`, `cargo check -p voxel_demo`, and byte-empty `git diff -- apps/voxel_demo/test_data/goldens/`. | `DECISION-20260721-01`, `DECISION-20260721-02`, `DECISION-20260722-01` | `voxel-demo-config-contracts.md`, `voxel-demo-config-regeneration.md` |
| ARCH-20260722-01 | Voxel v2 generation and CPU scene package | active | v2 uses directly derived named RNG streams, five stable core site IDs plus deterministic auxiliaries, a connected spline backbone, reserve-before-carve bounded maze planning, one operation-aware interior, and configured-thickness shell enforcement. MC33 output is classified by consistently wound world-space geometric normals into compact optional wall/floor meshes with independent UV scales. `CpuScenePackage` is renderer-free owned data and is the sole worker/headless handoff; renderer resource creation and procedural upload remain main-thread work. | `apps/voxel_demo/src/cave_gen/rng.rs`, `apps/voxel_demo/src/cave_gen/generators/topology_first.rs`, `apps/voxel_demo/src/meshers/`, `apps/voxel_demo/src/scene_package.rs` | Current mesh-package validation uses `AllowOpenEdges`; do not claim strict closed-manifold enforcement or a runtime asymptotic decider. Five site lights are mandatory and four midpoint lights are best-effort. Asset-digest storage exists but starts empty. | v2 fixture/hash tests, structural campaign tests, partition conservation/attribute tests, `cargo test -p voxel_demo`. | `DECISION-20260722-01`, `DECISION-20260722-02`, `DECISION-20260722-03`, `DECISION-20260722-04` | `voxel-v2-maze-planning.md`, `voxel-demo-pbr-texture-channels.md`, `voxel-demo-config-regeneration.md` |
| ARCH-20260722-02 | Voxel app-local editor and regeneration | active | Windowed v2 registers an existing renderer imgui app callback around an `EditorModel` complete draft and a bounded owned command queue. Registered UI suppresses app camera/gameplay input; F1/F2 toggle registration, and hidden UI restores routing. `RegenerationState` runs at most one CPU package worker with one latest pending request, discards stale results, and performs material creation, optional wall/floor uploads, stable-light updates, candidate-node commit, and rollback on the event-loop thread while the old package stays authoritative. | `apps/voxel_demo/src/editor.rs`, `apps/voxel_demo/src/regeneration.rs`, `apps/voxel_demo/src/materials.rs`, `apps/voxel_demo/src/main.rs` | GPU upload remains synchronous. Mesh unload transfers to renderer fence-aware retirement after detachment. App material/texture retirement currently uses active/cache protection plus a three-frame grace, not a fence-completion serial; do not describe it as a renderer transaction. Headless v2 has no editor or regeneration controller. | Editor command/lifecycle tests, regeneration stale/rollback/cache tests, `cargo test -p voxel_demo`, `cargo check -p voxel_demo`. | none | `voxel-demo-config-regeneration.md` |

## Drift Records

| id | spec | status | observed_drift | impact | routing | source | review_after |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DRIFT-20260719-01 | `ARCH-20260719-02` | resolved | Phase 03 candidate routing previously selected width envelopes on the structural map border, while Phase 04 requires border preservation. `OccupancyClass::Border` now reserves every layer perimeter before placement and topology treats it as non-walkable. | Fixed seed 77 now produces only interior ordinary witnesses/envelopes and successfully materializes through Phase 04. | GitHub issue #47 and `.internal-dev/bugs/.archive/phase-04-upstream-border-witness/report.md` | Phase 04 border-fix revalidation | 2026-07-19 |
