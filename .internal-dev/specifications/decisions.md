---
schema_version: 1
document_type: decisions-specification
status: active
owner: internal-dev
created: 2026-07-03
---

# Decisions

## Active Decisions

| id | decision | status | owner | source | decided_on | justification | alternatives_or_tradeoffs | caveats | affected_specs | knowledge_ref | review_after |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DECISION-20260703-01 | Use specifications as the living intended-truth store for engine internal-dev workflow. | active | internal-dev | user request | 2026-07-03 | Magenta2's upgraded workflow made specifications more precise than catch-all focus or notes files. | Keeping notes/focus as active destinations was simpler but caused weaker routing and stale guidance. | Existing legacy artifacts remain historical evidence until deliberately migrated or archived. | `workflow.md`, `architecture.md`, `api.md`, `web.md` | none | 2026-08-03 |
| DECISION-20260707-01 | Split renderer platform input handling from app-owned input dispatch. | active | runtime architecture | Phase 03 engine runtime abstractions implementation | 2026-07-07 | Apps need to dispatch caller-owned input and emit action events once per app frame without losing renderer-owned ImGui/debug/capture/cursor side effects. | Keeping all queueing inside `Renderer::update_input` preserves compatibility but prevents app-owned input streams; moving platform handling into root would invert the dependency boundary. | Renderer examples and legacy paths may keep `Renderer::update_input`; dogfood is now the migrated real-app proof. | `architecture.md`, `service-graph.md`, `services.md`, `api.md` | none | 2026-08-07 |
| DECISION-20260707-02 | Keep app lifecycle dispatch in root helpers over a caller-owned `EventBus`. | active | runtime architecture | Phase 04 engine runtime abstractions implementation | 2026-07-07 | The migrated app path needs one bus for lifecycle, input, and subsystem events without routing app mutation through renderer-owned state. | Wrapping the bus in a required runtime object would hide raw primitives; keeping lifecycle events in renderer APIs would duplicate app-owned `FrameStarted`/`FrameEnded` on the new path. | Renderer legacy frame APIs continue to emit renderer-owned lifecycle events for compatibility; dogfood now emits app-owned lifecycle/audio/input telemetry. | `architecture.md`, `service-graph.md`, `services.md`, `api.md` | none | 2026-08-07 |
| DECISION-20260707-03 | Keep root `engine` as both launcher binary and thin library facade. | active | runtime architecture | Phase 06 closeout | 2026-07-07 | The workspace needs a data-driven launcher and a convenient app-facing import surface, but support crates must remain directly usable and independently checkable. | Creating a heavier runtime crate or required app object would over-abstract the hobbyist engine; keeping only raw crates would make app-owned loops more repetitive. | The root facade may grow only as thin helpers/reexports; support crates and renderer must not depend on root `engine`. | `architecture.md`, `service-graph.md`, `services.md`, `api.md` | none | 2026-08-07 |
| DECISION-20260707-04 | Keep `CameraView` in the renderer facade and re-export it through root `engine`. | active | renderer architecture | Phase 02 and Phase 06 closeout | 2026-07-07 | The renderer consumes view/projection data at the render submission boundary, while apps need a simple DTO to pass app-owned camera state without giving renderer ownership of gameplay camera. | Putting the DTO only in root would create a bad renderer-to-root pressure; hiding it behind a broad runtime object would over-abstract the app loop. | `CameraView` must stay Vulkan-opaque and narrow. Future view/projection additions should be validated against dogfood and renderer examples. | `architecture.md`, `service-graph.md`, `services.md`, `api.md` | none | 2026-08-07 |
| DECISION-20260707-05 | Keep the app-owned workflow primitives narrow: fixed-step timing is retained, FPS binding convenience is deferred, and dogfood capture flag drift is quarantined. | active | runtime architecture | Core app workflow primitives Phase 00 | 2026-07-07 | A pure `FixedStepClock` is deterministic, independently testable, and useful without owning physics/gameplay; FPS binding helpers would add camera policy without removing meaningful dogfood duplication; dogfood capture parser equals-form/space-form drift is unrelated to app-owned loop primitives. | Dropping fixed-step would avoid API surface but lose a small reusable primitive; adding FPS bindings now risks policy creep; fixing capture parsing now would expand scope into parser compatibility. | Issue #39 remains queued only. Capture flag drift must stay documented until a focused parser compatibility fix. Renderer-owned compatibility APIs and examples are not removed by this decision. | `architecture.md`, `service-graph.md`, `services.md`, `api.md` | none | 2026-08-07 |
| DECISION-20260707-06 | Label all renderer-owned compatibility API docs with explicit "renderer compatibility path" and "current app-owned path" markers without removing any documentation. | active | internal-dev | Phase 01 docs compatibility labeling | 2026-07-07 | Readers of docs/api and docs/internal need to distinguish renderer-owned compatibility/demo paths (`update_input`, `render_scene`, `install_default_fps_input`) from current app-owned helpers (`route_platform_input_to_app`, `render_scene_with_view`, `CameraView`, `camera_view_for_size`) without losing compatibility reference content. | Removing compatibility sections would break renderer example coverage and migration staging; keeping them unlabeled causes confusion about which path is recommended for custom apps. | Compatibility sections are retained with explicit labels. No docs are removed. App-owned guidance leads with current path. This is a docs-only change with no API/behavior impact. | `api.md`, `architecture.md`, `services.md` | none | 2026-08-07 |
| DECISION-20260718-01 | Preserve one legacy-created scene directional light and one 2048² D32 comparison shadow map per frame slot in default builds, with direction defined surface-to-light. | active (compatibility baseline) | renderer architecture | Engine integration gate Phase 03 validation and repair | 2026-07-18 | The zero/one creator and frame-local map remain the deterministic compatibility baseline and rollback path. | Phase 08 adds bounded additive direct directionals and three-cascade CSM behind `csm`; atlases and point/spot shadows remain deferred. | `create_directional_light` still rejects a second legacy light. `add_directional_light` is the separate additive route. Default builds retain implicit legacy shadow ownership; CSM feature builds require explicit `DirectionalShadowConfig`. | `api.md`, `architecture.md`, `deferred-features.md` | `renderer-directional-shadow-behavior.md` | 2026-08-18 |
| DECISION-20260718-02 | Enable scene frustum culling by default while preserving an explicit public opt-out. | active | renderer architecture | Engine integration gate Phase 02 | 2026-07-18 | Default-on culling proves the submission path is exercised in normal use and removes known off-screen proxy-bounded draws. Independent descendant tests preserve visible children beneath off-screen parents. | Keeping culling opt-in would lower regression risk but leave the gate capability unused; waiting for true mesh/subtree bounds would defer a currently useful alpha optimization. | The Phase 02 unit-proxy limitation was resolved by Phase 06 conservative scene bounds (`DECISION-20260725-12`). The public opt-out remains for diagnostics, but dogfood no longer requires it. | `api.md`, `architecture.md` | `renderer-frustum-culling-behavior.md` | 2026-08-18 |
| DECISION-20260718-03 | Treat device loss and backend unwind/terminal failures as typed terminal renderer states, and require every post-acquire failure path to retire frame synchronization. | active | renderer architecture | Engine integration gate Phases 01 and 04 | 2026-07-18 | Hosts need actionable failure classification instead of process panics, while Vulkan fence/semaphore/image ownership must remain valid even when recording fails after acquisition. | Transparent device recovery would provide better availability but requires recreatable resource descriptions and substantially broader lifecycle work; retrying a poisoned backend is unsafe. | The first device-loss operation returns `RendererError::DeviceLost`; later backend work returns `BackendPoisoned`. Applications destroy and recreate the renderer. | `api.md`, `architecture.md` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` | 2026-08-18 |
| DECISION-20260718-04 | Copy material draw bindings into frame-local records while cache locks are held rather than storing raw cache pointers in render objects. | active | renderer architecture | Engine integration gate Phase 01 | 2026-07-18 | Cache vectors can move during mutation, so a raw pointer that outlives the lock is a lifetime hazard. The draw path needs only a small copyable subset of material state. | Holding the cache lock through all bucket use would avoid dangling pointers but broaden contention and couple recording lifetime to cache access; owning full material objects would copy unnecessary state. | New draw-time material fields must be added to `CopiedMaterialDrawRecord` and copied under the texture-cache guard. | `architecture.md` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` | 2026-08-18 |
| DECISION-20260725-01 | Staged, foundation-first sprint program: lifecycle guardrails before code movement, architectural seams before features, one evidence-backed vertical per family. No big-bang rewrite. | active | sprint architecture | Engine integration sprint handoff | 2026-07-25 | The sprint addresses five deferred capability families. Establishing lifecycle guardrails first prevents regression; architectural seams enable safe extraction; evidence-backed verticals prevent half-implemented features. | Delivering all features in parallel would risk regression and merge conflicts; a purely sequential program would delay independent verticals unnecessarily. | Phase 2 tracks are gated on Phase 0+1 evidence; a track whose gate fails is re-evaluated, not silently dropped. | `architecture.md`, `api.md` | none | 2026-08-25 |
| DECISION-20260725-02 | Command-recording extraction is the first architectural seam. File extraction is behavior-preserving; `VkRenderCore` remains the cross-subsystem coordinator; public `Renderer` facade does not change. | active | renderer architecture | Engine integration sprint handoff | 2026-07-25 | Extracting command recording before shadow/BVH/instancing work prevents `vk_render.rs` from growing unboundedly and enables pass-specific testing contexts. | Extracting all four subsystems at once would be higher risk; delaying extraction until after features would make features harder to implement cleanly. | Extraction follows lifecycle hardening tests. No trait abstraction while only one implementation exists. | `architecture.md` | none | 2026-08-25 |
| DECISION-20260725-03 | CSM is the first shadow family. Multi-light data groundwork establishes the infrastructure; cascaded directional shadows are the first increment. Spot and point cube shadows follow after CSM proves stable. | active | renderer architecture | Engine integration sprint handoff | 2026-07-25 | Delivering one complete shadow family proves the multi-light infrastructure and ABI migration without the complexity of multiple simultaneous shadow types. Phase 08 delivered the three-cascade CSM vertical behind the `csm` feature gate with per-cascade conservative culling, blend band, off-camera caster invariant, and synchronized `EnvironmentUBO` ABI. | Delivering all shadow types in one phase would create cross-cutting ABI and pipeline risk; deferring all shadows would leave the infrastructure untested. | Atlas, virtual shadow maps, and bindless descriptors are out of scope. Post-phase validation identified and repaired UBO wiring, descriptor selection, and API gaps. | `architecture.md`, `api.md`, `deferred-features.md` | `renderer-csm-cascade-fitting.md` | 2026-08-25 |
| DECISION-20260725-04 | Convex hull and static trimesh only for mesh colliders. No convex decomposition, collider LOD, or automatic scene walking. Collider recipes are per-mesh, generated at asset ingestion from neutral renderer DTO, and governed by explicit policy. | active | physics architecture | Engine integration sprint handoff | 2026-07-25 | Convex hull and trimesh cover the common collision cases without convex decomposition complexity. Generating recipes at ingestion avoids GPU readback. Phase 09 (2026-07-19) delivered the full app-local recipe bridge in `dungeon_dogfood` with all three policies, scale baking, shear rejection, and transform writeback. | Adding convex decomposition would require a third-party library and substantially more validation; automatic scene walking would entangle the bridge with scene-graph traversal. | The bridge remains app-local until a second consumer is confirmed. | `architecture.md`, `deferred-features.md`, `mesh_collider_bridge.rs` | none | 2026-08-25 |
| DECISION-20260725-05 | Bounds → BVH → LOD → instancing, in order. True conservative bounds are prerequisite infrastructure shared by all spatial subsystems. GPU Hi-Z occlusion is deferred due to unresolved previous-frame false-negative correctness. | active | renderer architecture | Engine integration sprint handoff | 2026-07-25 | Bounds are the common input to culling, shadows, BVH, and LOD. Building them first prevents each subsystem from computing its own (potentially inconsistent) bounds. | Computing bounds inside each subsystem would cause duplication and inconsistency; deferring bounds would make BVH and LOD depend on proxy estimates. | Hi-Z is deferred until a correctness-safe approach is designed. Instancing requires the command-recording seam. | `architecture.md`, `deferred-features.md` | `renderer-frustum-culling-behavior.md` | 2026-08-25 |
| DECISION-20260725-06 | Neutral geometry-export DTO before bridge crate. Renderer exports a plain DTO with collider-relevant geometry at asset ingestion. The bridge integration path is created only after the DTO contract is proven. | active | integration architecture | Engine integration sprint handoff | 2026-07-25 | The DTO is a narrow, Vulkan-free contract that prevents renderer→physics dependency and keeps the bridge replaceable. Proving it with sync/async/unload/cancel scenarios before bridge integration prevents late architectural surprises. | Putting collider generation inside the renderer would invert the dependency; creating a workspace bridge crate before the DTO is proven would prematurely freeze the API. | The bridge should remain an app-local module until a second consumer is confirmed. | `architecture.md`, `api.md` | none | 2026-08-25 |
| DECISION-20260725-07 | Feature flags for all new rendering paths. CSM, instancing, and new culling ship behind compilation and runtime feature flags. Flags are removed only after deterministic evidence passes. | active | renderer architecture | Engine integration sprint handoff | 2026-07-25 | Feature flags provide explicit rollback for every capability family. A flag that compiles but is runtime-inactive prevents partial execution paths. | Shipping new paths unconditionally would risk regressions in the default path; gating only at runtime would not prevent accidental code coupling. | Flags are exactly `csm`, `instancing`, and `scene-bvh`; all excluded from `default`. | `architecture.md` | none | 2026-08-25 |
| DECISION-20260725-08 | ABI manifest required before descriptor-set changes. Descriptor set 0 migration is a global compatibility event. An ABI manifest documenting the exact layout, bindings, and shader contracts must be produced before any changes. | active | renderer architecture | Engine integration sprint handoff | 2026-07-25 | The descriptor ABI is a cross-cutting contract between Rust layout code, Vulkan descriptor layouts, GLSL declarations, SPIR-V, and pipeline binding order. Changing it without a manifest risks silent drift. | Proceeding without a manifest would defer drift detection to runtime validation errors; documenting after changes would miss the pre-change baseline. | The manifest covers every descriptor set/binding from Rust and GLSL in current code. | `architecture.md` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` | 2026-08-25 |
| DECISION-20260725-09 | Separate deterministic and real-WSI validation matrices. Headless captures cannot validate WSI lifecycle. A separate real-WSI integration matrix covering resize, surface loss, and present error paths is required. | active | validation architecture | Engine integration sprint handoff | 2026-07-25 | WSI lifecycle (resize, out-of-date, suboptimal, surface loss, present failure) exercises code paths that headless rendering cannot reach. A separate matrix makes the gap explicit. | Combining both into one matrix would conflate deterministic rendering evidence with environment-dependent WSI behavior; omitting the WSI matrix would leave those paths unvalidated. | Phase-tagged validation only. Unavailable WSI entries are marked unexecuted with environment evidence. | `architecture.md` | none | 2026-08-25 |
| DECISION-20260725-10 | Pure-Rust glTF as primary import path. Inventory russimp-sys formats. Prefer a pure-Rust glTF solution for the primary path. Retain Assimp behind an optional feature flag. | active | asset architecture | Engine integration sprint handoff | 2026-07-25 | A pure-Rust glTF importer reduces build complexity, improves compile times, and avoids C dependency chain issues. Assimp remains for formats glTF cannot cover. | Removing Assimp without an evidence-backed replacement would break non-glTF format support; keeping Assimp as the only path would retain the C dependency chain for all formats. | Do not change importer ownership based only on the assessment. | `architecture.md`, `deferred-features.md` | none | 2026-08-25 |
| DECISION-20260725-11 | Slot+generation handles require retirement contract. Handles do not intrinsically solve in-flight lifetime. An explicit retirement contract — when a handle may be freed relative to GPU frame completion — must be specified and tested. | active | renderer architecture | Engine integration sprint handoff | 2026-07-25 | Slot+generation prevents use-after-invalidate but does not prevent use-after-free on the GPU. A fence-aware retirement contract closes this gap for bounds entries, collider recipes, BVH leaves, LOD chains, and instance records. | Deferring the contract would leave every new slot+generation store with an undefined safety boundary; implementing it ad-hoc per store would cause inconsistency. | Phase 04 adds the fence-aware delayed reuse rule; Phase 01 only establishes the contract documentation. | `architecture.md` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` | 2026-08-25 |
| DECISION-20260725-12 | Conservative-visibility default for unknown bounds. Any node without trusted geometry bounds — missing mesh, skinned, deformed — is marked conservatively visible. Proxy bounds are used only when geometry is known-unavailable, not as a substitute for uncomputed bounds. Implemented via `SceneBounds::{Known, Proxy, ConservativeVisible}`, `BoundsUnknownReason`, `MeshBoundsEntry`, `Scene::add_mesh_with_bounds`, `AssetManager::mesh_scene_bounds`, `Aabb::transformed` (8-corner), post-order subtree aggregation, and `RetirementClass::BoundsEntry` in Phase 06. | active | renderer architecture | Engine integration sprint handoff | 2026-07-25 | The engine-alpha uses unit proxy AABBs for all nodes. True bounds must distinguish known rigid geometry from unknown/deformed/skinned, and unknown must never be falsely culled. | Keeping proxy bounds for all nodes would preserve the status quo but prevent accurate culling; requiring bounds for all nodes would block scenes with skinned or streaming geometry. | A unit proxy is not an acceptable substitute for an uncomputed known mesh AABB. | `architecture.md` | `renderer-frustum-culling-behavior.md` | 2026-08-25 |
| DECISION-20260725-13 | Descriptor reset authorization must be a single-use type/value (`CompletedFrameSlot`) created only after successful fence completion, not an unchecked boolean. The token is validated (slot identity + exact expected frame serial + epoch monotonicity) before any Vulkan call and consumed on first use. Partial reset failure quarantines all tracked pools. | active | renderer architecture | Engine integration sprint Phase 02 | 2026-07-25 | Fence-authorization prevents descriptor reset while the GPU still owns in-flight descriptors. Exact serial matching prevents a valid completion observation from being applied to the wrong frame epoch. Quarantine prevents reuse when only a prefix of pools physically reset. | Using a bare boolean would be easy to misuse (accidental reuse, no compile-time tracking). Using a global lock would couple frame epochs unnecessarily. Retaining ready records after partial reset would expose ambiguous physical state. | The token type and constructor are crate-private, tokens reject duplicates via `take()`, and fault injection remains a module-private adapter rather than a device trait in the facade. | `architecture.md` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` | 2026-08-25 |
| DECISION-20260725-14 | Swapchain lifecycle must be modeled as an explicit state machine with irreversible old-generation retirement, not an `Option<VkSwapchain>` plus a boolean flag. Classification of WSI outcomes must be structural (enums), not string-based. | active | renderer architecture | Engine integration sprint Phase 03 | 2026-07-25 | A boolean `resize_requested` cannot distinguish out-of-date from surface-lost, coalesce resize requests, or enforce that a retired oldSwapchain is never reused. Structured classification prevents silent loss of suboptimal/surface-lost information. | Keeping the boolean + Option model would require ad-hoc error recovery in every call site; moving the entire Vulkan lifecycle into a single monolithic type would prevent unit testing. | `device_wait_idle` remains in rebuild for pre-retirement drain but is not a substitute for ownership modeling. Real-WSI evidence is pending. | `architecture.md` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` | 2026-08-25 |
| DECISION-20260725-15 | Fence-aware handle retirement: every slot+generation store must use a generic retirement queue keyed by `FrameSerial`. Invalidation is immediate; payload destruction and slot reuse are deferred until `completed_serial >= retire_after`. Completion advances only from fence observations. | active | renderer architecture | Engine integration sprint Phase 04 | 2026-07-25 | Slot+generation prevents use-after-invalidate but not use-after-free on the GPU. A single retirement vocabulary — `FrameSerial`, `RetirementClass`, `GpuRetirementQueue<T>` — ensures every new slot+generation store (bounds, collider recipes, BVH leaves, LOD references, instance records) encodes the fence-safety invariant once instead of per-store. | Tying retirement to descriptor-pool reset timing without an explicit serial would couple two independent lifecycle concerns; per-store ad-hoc retirement would risk inconsistency across mesh, bounds, collider, BVH, LOD, and instancing stores. | The retirement queue is generic over `T`; type-specific destruction is caller-owned. Reserved default slots remain non-retirable. Generation wrap uses `checked_add` with error return, not silent wrap. | `architecture.md`, `api.md` | `renderer-vulkan-lifecycle-and-audit-gotchas.md` | 2026-08-25 |
| DECISION-20260719-01 | Represent every ramp transition with two pre-placed endpoint regions and one reservation-bound vertical candidate edge; keep hub projection occupancy distinct from protected transition volume and build the candidate graph only after placement. | active | dungeon generator architecture | Phase 03 data-flow redesign | 2026-07-19 | A transition-owned ordinary footprint made endpoint placement impossible, while same-layer-only compatibility made transition independence unreachable. Explicit endpoint IDs/socket IDs plus `TransitionHub` preserve ownership without blocking the transition's own regions, and a separate occupancy-backed candidate graph gives topology selection actual vertical edges and legal rerouting context. | Treating the ramp hub as one multi-layer region would hide graph endpoints; synthesizing vertical edges during final selection would detach proofs from prefab reservations; allowing ordinary regions over generic transition cells would weaken ownership. | Hub footprints are projections, not final tiles. Only ramp run, upper opening, landing, and headroom use protected transition occupancy. Corridor conflicts are rerouted first and otherwise consume the configured crossing budget. | `architecture.md` | `dungeon-generator-phase-validation.md` | 2026-08-19 |
| DECISION-20260720-01 | Replace legacy thin-quad floor/wall/ramp/ceiling geometry emitters with closed volumetric primitives (floor slabs, wall boxes, ramp wedges, ceiling closures) in a single-pass `emit_tile_geometry` traversal. Remove comparison hooks, directional wall-face culling logic, and individual `emit_` functions once the volumetric system proves equivalent. | active | dungeon geometry architecture | Phase 01-06 geometry conversion | 2026-07-20 | Thin quads required complex neighbour-aware face-culling heuristics and directional ramp-cap logic that was error-prone and produced visible gaps at wall-ramp junctions. Closed volumetric primitives eliminate those heuristics by relying on same-primitive adjacency culling and surface-height clipping, while single-pass traversal removes the need for a `SubLayer` enum and domain-switching logic. | Keeping thin quads with incremental fixes (comparison hooks, SubLayer-driven face selection) would require indefinitely maintaining two parallel geometry paths; removing quads wholesale and implementing volumetric primitives from scratch was more auditable and produced higher-quality geometry. | Ramp inference (`inferred_ramp_ceiling_openings`) is preserved for ramp-run ceiling suppression; generic upper-layer `Void` does not trigger it. The `MULTILAYER_SURFACE_GAP` constant and `emit_ceiling_for_tile` remain for ramp ceiling emission between layers. | `architecture.md` | `dungeon-generator-phase-validation.md` | 2026-08-20 |
| DECISION-20260721-01 | Create `apps/voxel_demo` as a permanent workspace member with a topology-first procedural cave generator, MC33 (Marching Cubes 33) isosurface mesher, and PBR stone rendering. Default resolution 96³. App owns its input, camera, frame capture, and event loop. Resolution gated to 64/96/128. Point lights capped at 16. No physics, audio, or scripting dependency. | active | voxel demo architecture | Voxel Cave Demo sprint (Phases 01–05) | 2026-07-21 | A standalone procedural-cave demo app proves the engine's procedural mesh pipeline, app-owned rendering path, and headless capture validation on a non-dungeon domain. It exercises `ProceduralMeshData` upload, PBR materials, point lights, IBL, and manual/headless frame captures in a self-contained crate with no dungeon or physics coupling. | **Why topology-first**: places semantic sites (spawn, junction, grand_cavern, shaft, destination) before carving connecting routes, producing navigable cave topology. Noise-threshold approaches (just FBM cutoff) produced disconnected pockets and no site guarantee. **Why MC33**: Lewiner et al. base tables embed 33 distinct case configurations that reduce hole incidence compared to the classic 15-case table, even without a runtime asymptotic decider (not implemented). Surface Nets were rejected because they produce non-manifold edges on cave fields. **Why 96³ default**: 64³ is too coarse for room-scale features; 128³ requires 8× the memory of 64³ and slows mesh extraction. 96³ balances shape fidelity and iteration speed. **Why app-owned loop**: follows `ARCH-20260707-03` and `ARCH-20260707-06` — app owns input/camera/frame capture, renderer provides `render_scene_with_view` and `render_scene_headless`. | Voxel demo shares no code with dungeon_dogfood. IBL uses a fallback path to `apps/dungeon_dogfood/assets/sky_maps/`. Generator parity golden files (seeds 1–12) live in `apps/voxel_demo/test_data/goldens/`. | `architecture.md` | none | 2026-08-21 |
| DECISION-20260721-02 | Generator v2 returns a parallel persisted result with canonical u8 site IDs, core/auxiliary roles, owned labels, route kinds, actual-route clearance, five core viewpoints, and nine light anchors; v1 `GeneratorResult` remains unchanged. | active | voxel generator architecture | Phase 02 blocker remediation | 2026-07-21 | A parallel result preserves immutable v1 fixtures while making v2 output replayable and invariant-checkable. Actual centerline clearance represents warped spline and maze geometry; endpoint chords do not. | Extending the shared v1 result would be smaller but would couple legacy fixtures to v2 schema. Anchors at guaranteed carved core/route cells favor deterministic safety over presentation-specific offsets. | Fixture hashes are blessed for canonical presets normalized to 64³ on the recorded toolchain/platform. Later phases may add raw fixture bytes without replacing this manifest. | `architecture.md` | `voxel-v2-maze-planning.md` | 2026-08-21 |
| DECISION-20260722-01 | Derive every v2 random stream directly from `(seed, rng_version, semantic stage tag)` with SHA-256 framing instead of consuming a shared master stream. | active | voxel generator architecture | Configurable regeneration sprint Phase 02 and Phase 08 closeout | 2026-07-22 | Direct named streams make unrelated-stage insertion/reordering independent and keep site/link/attempt randomness attributable and replayable. Canonically ordered inputs plus stable tags preserve deterministic output without parallelizing order-sensitive work. | A single sequential RNG is simpler and cheaper to seed, but any added draw perturbs every later stage. Hierarchical nonce streams retain hidden consumption-order coupling. Named streams add hashing and make tag spelling part of output versioning. | Applies only to v2; v1 `PhaseTaggedRng` and goldens remain unchanged. Renaming a tag or changing framing requires fixture/identity review. | `architecture.md`, `api.md` | `voxel-v2-maze-planning.md`, `voxel-demo-config-regeneration.md` | 2026-08-22 |
| DECISION-20260722-02 | Plan and reserve the complete requested v2 maze set before carving any accepted maze route. | active | voxel generator architecture | Configurable regeneration sprint Phase 02 and Phase 08 closeout | 2026-07-22 | Reserve-before-carve makes maze exhaustion failure-atomic, prevents later routes from silently intersecting earlier future carve footprints, and enables deterministic substitution under one global bounded search budget. | Carving each route immediately uses less temporary memory but leaks partial maze state on later exhaustion and makes retry/substitution order observable. Reserving only center cells is cheaper but does not protect the actual radius footprint. | Endpoint overlap is an explicit zone policy. Search/reconstruction work is globally charged; a failed candidate is skipped deterministically, and whole generation still commits through a candidate `VoxelWorld`. | `architecture.md`, `api.md` | `voxel-v2-maze-planning.md`, `voxel-demo-config-regeneration.md` | 2026-08-22 |
| DECISION-20260722-03 | Partition validated MC33 triangles into optional wall and floor meshes using consistently wound world-space geometric normals, then compact each bucket in first-reference order with independent UV scales. | active | voxel mesh architecture | Configurable regeneration sprint Phase 03 and Phase 08 closeout | 2026-07-22 | The renderer binds one material per procedural mesh, so geometric partitioning provides distinct wall/floor PBR materials without renderer, shader, descriptor, per-triangle-material, or voxel-material propagation changes. Triangle conservation and order-preserving compaction keep output auditable and deterministic. | One mesh/one material cannot distinguish floors. Renderer per-triangle materials or a shader classifier would expand renderer/descriptor scope. Using interpolated vertex normals can misclassify hard slopes; dormant voxel material tags do not encode the required surface semantics. | Empty partitions are not uploaded. Classification depends on the effective object-to-world transform (identity in this app). The current package uses `AllowOpenEdges`; this decision does not claim strict closed-manifold enforcement. | `architecture.md`, `api.md` | `voxel-demo-config-regeneration.md` | 2026-08-22 |
| DECISION-20260722-04 | Bind KB3D ambient occlusion from its separate linear `_ao.png` texture to `ao_tex`, keep separate linear roughness in `metallic_roughness_tex`, and force metallic factor to zero. | active | voxel material architecture | Configurable regeneration sprint Phase 04 and Phase 08 closeout | 2026-07-22 | The renderer samples AO from R in its dedicated slot and glTF roughness from G in the metallic-roughness slot; grayscale decoding replicates authored roughness across channels. Separate source textures preserve authored AO and roughness without importing metallic data. | Loading `_arm.png` would couple AO/roughness/metallic and risk nonzero metallic. Reusing roughness as AO or omitting AO loses authored occlusion. Shader/descriptor changes are unnecessary and out of scope. | Base color remains sRGB while normal/roughness/AO are linear. `_arm.png`, `_metallic.png`, and `_height.png` stay excluded. KB3D redistribution/licensing approval remains external. | `architecture.md`, `api.md` | `voxel-demo-pbr-texture-channels.md`, `voxel-demo-config-regeneration.md` | 2026-08-22 |
| DECISION-20260722-05 | Guide → API → Internal three-tier documentation architecture. | active | documentation architecture | Phase 06 guide documentation implementation | 2026-07-22 | End-user documentation (`docs/guide/`) addresses a distinct audience from the existing API reference (`docs/api/`) and internal implementation docs (`docs/internal/`). The guide presents a cumulative learning path in numbered chapters, teaches the app-owned loop as the primary custom-app pattern, and links to API docs for reference details and internal docs for maintainers. Each tier has a clearly stated audience: Guide for app builders, API for runtime contract readers, Internal for engine maintainers. | A single unified doc could avoid tier confusion but would bury beginner content and mix maintenance contracts with tutorial prose. Adding guide material to the existing API docs would overload chapters that already serve as reference. | Guide chapters must not duplicate API reference material; prefer links. Guide prose must match the maintained checkpoint exactly. | `api.md` | none | 2026-08-22 |
| DECISION-20260722-07 | Execute the engine safety-refactor stabilization sprint as a phased program with evidence-required gate progression, finding matrix as canonical source, and steward integration. | active | sprint architecture | Phase 01 execution baseline and decision gates | 2026-07-22 | The 2026-07-22 comprehensive code review identified 33 findings (C1-C8, H1-H12, M1-M8, L1) across four priority levels. A phased approach with per-phase evidence, validator PASS, and steward integration ensures each finding is addressed without contention on shared records. | A single monolithic phase would risk merge conflicts and unvalidated refactors; addressing findings ad-hoc without a canonical matrix would lose traceability; allowing parallel phases to edit the matrix directly would cause contention. | L1 and M2 are deferred. M findings activate only when linked to a required C/H invariant, compatibility defect, observable-error contract, or validation need. Every phase writes evidence packets; the steward applies row transitions and specification deltas after validator PASS. | `architecture.md`, `api.md`, `services.md`, `service-graph.md` | none | 2026-08-22 |
| DECISION-20260722-08 | The finding matrix at `.internal-dev/plans/engine-safety-refactor-stabilization/finding-matrix.md` is the canonical inventory. Review assertions are historical evidence; execution-baseline revalidation is required. Phase implementers write evidence packets; the steward applies row transitions. | active | sprint architecture | Phase 01 finding matrix creation | 2026-07-22 | A single canonical matrix prevents forked interpretations between phases. Phase implementers write phase-specific evidence without contending on the shared matrix; the steward merges after validator PASS. | Keeping findings only in the review synthesis would leave no current-status tracking; per-phase matrices would diverge. | The matrix preserves baseline history. A later phase must not overwrite the Phase 01 baseline status; it proposes changes through its evidence packet. | `architecture.md` (`ARCH-20260722-03`) | none | 2026-08-22 |
| DECISION-20260722-09 | M findings (M1-M8) activate only when current evidence links them to a required C/H invariant, compatibility defect, observable-error contract, or validation need. M2 is deferred entirely. | active | sprint architecture | Phase 01 finding triage | 2026-07-22 | Not all M findings are prerequisites for C/H resolution. Activating every M finding would expand scope without proportional safety gain. Linking activation to C/H dependencies keeps the sprint focused on safety and correctness. | Activating all M findings would add scope without clear safety justification; deferring all M findings would miss real dependencies (M3 rendergraph ties to H1 sync, M5 capture ties to Gate 3 observability, M7 buffers[0] ties to H1 frame indexing). | Each M finding's matrix row records the activation justification when a phase claims it. M2 (god modules) and L1 (cleanup) are deferred entirely. | `architecture.md` (`ARCH-20260722-03`) | none | 2026-08-22 |
| DECISION-20260722-06 | Preserve renderer-free `engine_pack new-app` scaffold plus separate maintained renderer fixture (`examples/guide_app`). | active | tooling + documentation architecture | Phase 06 guide documentation implementation | 2026-07-22 | The existing `engine_pack new-app` generates a standalone support-crate scaffold with `engine_events`, `input`, and `physics` but no renderer. That scaffold remains the correct zero-Vulkan compile-and-run starting point. A separate maintained fixture (`examples/guide_app`) provides the explicit windowed continuation that adds the `engine` dependency (and therefore the renderer). | Adding renderer to the `new-app` template would force every scaffold user to install Vulkan SDK. Removing `new-app` would remove the only off-workspace compile-first entrypoint. A single combined scaffold would conflate two distinct onboarding stages. | `examples/guide_app` is a nested workspace, not added to root `Cargo.toml`. Its lockfile is source-controlled. The guide tells readers to run `engine_pack new-app` first, then shows the checkpoint as the explicit windowed next step. | `api.md` | none | 2026-08-22 |

| DECISION-20260723-01 | Adopt one normative Q1-family BSP profile (`q1-portable-ericw`): classic BSP29 as portable core, ericw-tools BSP2 + BSPX colored lighting as named TrenchBroom profile. Explicitly exclude BSP30/HL, BSP38/Q2, BSP46/Q3, Source/VBSP, and direct .map loading. | active | bsp-beta architecture | BSP Map Support Beta Phase 01 | 2026-07-23 | Depth over breadth: one complete, fixture-backed Q1-family profile with explicit diagnostics for unsupported dialects. The named ericw-tools profile covers the vast majority of community Q1 maps. | Supporting multiple BSP families simultaneously would spread evidence thin and risk false compatibility claims; a generic "BSP loader" that guesses dialects from filenames would be unreliable. | Unsupported dialects produce stable `BSP-UNSUPPORTED-DIALECT` errors, not best-effort fallback. | `bsp-compatibility.md` | none | 2026-08-23 |
| DECISION-20260723-02 | Dedicated RGB multi-style lightmap material path separate from PBR sampler ABI. BSP lightmaps are baked lighting data, not AO. A separate descriptor set and pipeline ABI prevents coupling BSP-specific requirements to the general PBR path. | active | bsp-beta renderer | BSP Map Support Beta Phase 01 | 2026-07-23 | The procedural material facade assigns runtime-created textures to UV0. BSP requires a deliberate RGB, multi-style lightmapped path. Separating the ABI avoids contaminating the PBR path with BSP-specific sampler requirements (style-indexed array textures, fullbright masks). | Reusing the AO slot for monochrome lightmaps was rejected as the beta endpoint because it cannot support colored lighting, multiple styles, or fullbright masks. Extending the existing PBR descriptor sets would risk silent ABI drift. | Transfer semantics are governed by `DECISION-20260727-01`; the dedicated descriptor ABI remains unchanged. | `bsp-renderer-lighting.md`, `bsp-compatibility.md` | none | 2026-08-23 |
| DECISION-20260723-03 | One `QuakeToEngine` coordinate transform `(x, y, z) -> scale * (x, z, -y)` with default scale `0.0254` governs all subsystems. Overridable per-package; cache-key includes scale. | active | bsp-beta spatial | BSP Map Support Beta Phase 01 | 2026-07-23 | A single authoritative transform prevents inconsistency between rendering, collision, queries, and entity placement. The default scale treats engine/glTF units as meters. | Per-subsystem transforms would risk misalignment between render and collision geometry. A fixed built-in scale would prevent non-meter content. | Angle sentinel conversion, plane distance remapping, and inline-model pivot rebasing are fixture-backed proof requirements. | `bsp-spatial-physics.md` | none | 2026-08-23 |
| DECISION-20260723-04 | Hybrid spatial model: retain BSP trees, PVS, and clip queries for visibility and specialized traces while converting render geometry and engine physics to the engine's native representations (BVH/frustum culling for submission, Rapier for dynamics). | active | bsp-beta spatial | BSP Map Support Beta Phase 01 | 2026-07-23 | BSP PVS is superior to frustum culling for densely occluded indoor scenes; BSP clipnode traces provide Quake-compatible movement semantics for app-owned character controllers. Converting everything to BVH/Rapier would lose these optimizations; keeping everything BSP-native would prevent engine-standard lighting and physics. | A pure-BSP approach would require a separate rendering pipeline, lighting system, and physics engine, effectively forking the engine. A pure-BVH approach would lose PVS culling efficiency and hull-trace compatibility. | Corrupt VIS falls back conservatively to frustum/BVH (never consumes partially decoded visibility). | `bsp-spatial-physics.md` | none | 2026-08-23 |
| DECISION-20260723-05 | Transactional BSP publication: one integration coordinator (`src/bsp_runtime/`) performs generation-checked prepare/validate/commit/rollback. No partially mounted maps or cache residue remain after failure or cancellation. | active | bsp-beta lifecycle | BSP Map Support Beta Phase 01; Phase 05 revalidation | 2026-07-23 | A BSP load creates resources across renderer (scene nodes, materials, lightmap atlases, lights), physics (colliders), and app (behavior state). All must appear atomically or roll back completely. Generation tokens prevent stale loads from publishing. | Loading resources incrementally with separate fallback paths would risk partially visible scenes and leaked cache entries. A single monolithic load without generation checks would race with unload/reload. | Prepare duration must not hold locks across I/O. Commit is non-fallible after validation passes. The intended fence-aware GPU retirement remains unimplemented for BSP mount cancellation/replacement/unload because Scene detachment has no renderer queue acknowledgement (GitHub #59). | `bsp-transaction-ownership.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-06 | App-owned physics and behavior bridges. The BSP coordinator exports neutral DTOs; the app bridge creates Rapier objects and behavioral state. Renderer never owns physics objects; BSP crate never owns gameplay state. | active | bsp-beta integration | BSP Map Support Beta Phase 01; Phase 05 revalidation | 2026-07-23 | Gameplay behavior (door timing, button activation, trigger responses) is inherently app-specific. Physics objects (collider shapes, body kinds) are app policy decisions. The BSP crate provides only format knowledge and query data. | Putting behavioral state in the BSP crate would couple format parsing to game logic. Putting collider creation in the renderer would invert the renderer-to-physics dependency. | The current prepare/validate/commit/rollback token interface lacks a move-only active teardown receipt after commit; generic bridge unload/replacement is blocked by GitHub #60. Poisoning on bridge panic remains required. | `bsp-transaction-ownership.md`, `bsp-spatial-physics.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-07 | Source-linked scene asset: scene persistence stores BSP source reference (asset ID, content hash, compiler provenance, import settings, override layer), not an expanded copy of generated world nodes. | active | bsp-beta persistence | BSP Map Support Beta Phase 01 | 2026-07-23 | Persisting an expanded scene tree would lose the connection to the original BSP source, making reimport, override reconciliation, and identity matching impossible. The source reference preserves all metadata needed to reconstruct the scene. | Storing expanded nodes would avoid re-import time but would break override reconciliation and source-link identity. A hybrid approach (expanded cache + source reference) would risk stale expanded data. | UUID-based entity identity is accepted only with compiler-preservation evidence. Without UUID proof, identity uses best-effort fingerprint reconciliation. | `bsp-transaction-ownership.md`, `bsp-compatibility.md` | none | 2026-08-23 |
| DECISION-20260723-08 | Feature-gate BSP support behind `bsp` Cargo feature, excluded from `default`. Default engine builds must not link `bsp` or `bsp_runtime`, create BSP resources, or load BSP shaders/pipelines. | active | bsp-beta feature gating | BSP Map Support Beta Phase 01 | 2026-07-23 | Following `DECISION-20260725-07` (feature flags for new rendering paths), BSP support is an opt-in beta capability. Default builds preserve existing non-BSP behavior and regression baselines. | Shipping BSP unconditionally would risk startup-time and frame-path overhead for non-BSP users, and would require BSP shader SPIR-V in default shader manifests. A runtime-only flag would not prevent link-time coupling. | BSP feature is additive. Compiled-but-inactive builds must not create BSP resources or perform BSP frame-path work. Non-BSP capture baselines must remain within approved tolerances. | `bsp-acceptance.md` | none | 2026-08-23 |
| DECISION-20260723-09 | Entity identity through structural fingerprinting only. `_tb_id` UUID is stripped by ericw-tools qbsp and is not viable. Identity uses `(classname, origin, targetname, target)` fingerprint with duplicate ordinals. | active | bsp-beta persistence | Phase 01 fixture evidence | 2026-07-23 | Compiled BSP entity lumps from ericw-tools 2.0.0-alpha3 contain zero `_tb_id` keys. The compiler strips unknown entity key/value pairs. Structural fingerprinting is the only reliable identity mechanism for this compiler version. | A different Q1 compiler might preserve `_tb_id`. Supporting that would require compiler-specific identity detection. | Entity reconciliation always falls back to fingerprint matching. UUID path is retained in code only as a future hook for a different compiler that preserves entity keys. | `bsp-compatibility.md`, `bsp-transaction-ownership.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-10 | BSP2 field widths are uniformly widened in ericw-tools 2.0.0-alpha3: nodes 44B (children i32, mins/maxs i32, face_id/face_num u32), leaves 44B (mins/maxs i32, mark/markleaf u32), clipnodes 12B (children i32), edges 8B (u32×2). Faces/markfaces/surfedges widen to 28/4/4 bytes when non-empty. Models stay 64B. Widths verified via authoritative `bspinfo` tool. | active | bsp-beta compatibility | Phase 01 fixture byte inspection + bspinfo | 2026-07-23 | The `bspinfo` tool from ericw-tools reports authoritative element counts. BSP2 node lump 264B ÷ 6 nodes = 44B/node; leaf lump 308B ÷ 7 leaves = 44B/leaf; clipnode lump 288B ÷ 24 clipnodes = 12B/clipnode. All three are uniformly widened from BSP29 24/28/8. | Earlier analysis assumed BSP29-width fields in BSP2 (24/28/8) by incorrectly computing element counts from BSP29 strides. The authoritative `bspinfo` tool corrected the counts. | Parser must compute element width as `lump_size / element_count` and use widened widths for empty lumps (faces, markfaces). | `bsp-compatibility.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-11 | BSP companion precedence: explicit package manifest > BSPX RGBLIGHTING > .lit > base monochrome. Mismatch in .lit payload size vs base lightmap luxel count is a `CompanionContentMismatch` diagnostic. | active | bsp-beta compatibility | Phase 01 design | 2026-07-23 | Deterministic resolution prevents ambiguity when multiple colored-light sources are present. BSPX embedded data is always preferred over external .lit files. | A last-write-wins or "first-found" rule would cause non-deterministic behavior. | Both BSPX and .lit are unverified by current fixtures (no faces). Precedence order is design-specified, not fixture-proven. | `bsp-compatibility.md`, `bsp-renderer-lighting.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-12 | Frozen lighting contract: fullbright is per-pixel emission added to normal lighting, not a replacement. Four style slots per face, sentinel 255 unused. Alpha test writes depth, not transparent. Sky depth-tests without overwriting nearer depth. Liquid two-sided, depth-tested, blended, non-depth-writing. Dynamic-light receive masks, overbright 2.0, double-light prevention frozen. | active | bsp-beta renderer | Phase 01 contract closure | 2026-07-23 | Explicit frozen contract prevents implementation drift and gives dependent phases a stable ABI target. Every value has an approved owner decision. | Keeping values configurable would allow flexibility but risk ABI churn during implementation. | Calibration capture against reference renderer is pending a face-visible fixture. The contract values are design-approved but not yet calibration-verified. | `bsp-renderer-lighting.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-13 | Source-reference persistence: scene files store BSP identity (asset ID, content hash, compiler provenance, import settings, override layer), not an expanded copy of generated world nodes. GPU handles, descriptors, allocations, cache slots, transient generation handles, and expanded geometry are banned from serialization. | active | bsp-beta persistence | Phase 01 contract closure | 2026-07-23 | Persisting an expanded scene tree would lose the connection to the original BSP source, making reimport, override reconciliation, and identity matching impossible. The source reference preserves all metadata needed to reconstruct the scene. | Storing expanded nodes would avoid re-import time but would break override reconciliation and source-link identity. | UUID-based identity is not viable (compiler strips `_tb_id`). Fingerprint-based identity with duplicate ordinals is the approved reconciliation mechanism. | `bsp-transaction-ownership.md`, `bsp-compatibility.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-14 | BSP commit protocol is pure publication. After validation passes, commit must not parse, resolve packages, load external assets, allocate GPU resources, upload, look up handles, serialize, validate bridges, or reserve app-world capacity. Scene light-capacity preflight and source-link JSON serialization happen during validation, not commit. | active | bsp-beta lifecycle | Phase 05 atomic publication | 2026-07-23 | Commit purity ensures the atomic swap from hidden candidate → active scene cannot fail partway through with partially published state. Pre-serializing source-link JSON and preflighting light capacity during validation removes the last fallible operations from commit. | Putting serialization/validation inside commit would risk partial publication on failure. Deferring preflight to commit would require rollback of already-published scene state, which is not atomic. | Failure before commit leaves the active scene unchanged. Bridge commit panics still poison the coordinator. | `bsp-transaction-ownership.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260723-15 | In-flight BSP descriptors are never mutated. Frame-varying state (style intensities, animation indices, liquid parameters) uses dedicated set 2 UBO written once per frame after fence-wait. Static textures use one array layer — animation frame changes are communicated via uniforms, not descriptor rewrites. | active | bsp-beta renderer | Phase 06 surface/animation validation | 2026-07-23 | This prevents descriptor update hazards between GPU in-flight frames. It mirrors the existing PBR frame-slot pattern where per-frame data is written through freshly acquired buffer memory, not by mutating bound descriptors. | Writing per-frame values through descriptor updates would risk tearing across in-flight frames and add synchronization complexity. | Set 2 descriptors are per-frame-slot; the UBO memory backing them is written once per frame max. | `bsp-renderer-lighting.md` | `bsp-beta.md` | 2026-08-23 |
| DECISION-20260724-01 | External BSP normal/gloss companions use dedicated BSP PBR opaque/alpha-mask pipelines while preserving the three-set BSP descriptor ABI. Set 1 binding 1 packs R fullbright, G/B tangent normal X/Y, and A gloss; baked lightmaps remain diffuse-light modulation and explicit PBR materials opt into specular-only environment IBL. | active | bsp-beta renderer | user-requested external BSP PBR companions | 2026-07-24 | Packing unused legacy material-data channels adds authored PBR response without adding descriptors or coupling BSP lightmap/style semantics to the general mesh PBR sampler ABI. Keeping baked modulation as the diffuse-light input avoids double-lighting and preserves authored map lighting. | Adding separate normal/gloss descriptors would change the frozen ABI; routing through general mesh PBR would lose BSP lightmap/style/fullbright semantics. Two-channel normals cannot represent negative tangent Z, and fixed dielectric F0/metallic 0 is less general than a full material model. | Only opaque/alpha-mask surfaces are eligible. Missing channels use flat-normal/fully-rough defaults; no companions preserve legacy bytes and shader route. Transfer semantics are governed by `DECISION-20260727-01`; the specular IBL opt-in remains an additive exception to sealed legacy receive-mask defaults. | `bsp-renderer-lighting.md`, `bsp-compatibility.md`, `bsp-acceptance.md` | `bsp-beta.md` | 2026-08-24 |
| DECISION-20260724-02 | Generated dungeon output is BSP2 only. BSP29 has structural limits (65,535 vertices, edges, etc.) that M2 maps may approach or exceed. All compiler evidence PASS cells are BSP2. The generator must produce `-bsp2` compatible `.map` output exclusively. | active | bsp-dungeon architecture | BSP Dungeon Contract Evidence Phase 09 | 2026-07-24 | BSP2 has effectively unlimited indices and is the proven baseline (CMP-STD-BSP2, CMP-REPRO, CMP-PROVENANCE all PASS on BSP2). BSP29 has no compiler-produced face-visible fixture and is a diagnostic control only. | Supporting both BSP29 and BSP2 output would require separate compilation profiles, separate ceiling validation, and separate evidence cells without proportional benefit. | The compiler profile (`-bsp2`, `-threads 1 -lit`) is the same profile used for evidence fixtures. BSP29 remains supported for loading existing community maps but is not a generator output target. | `bsp-dungeon-generation.md`, `bsp-compatibility.md` | `bsp-beta.md` | 2026-08-24 |
| DECISION-20260724-03 | Open arches only for dungeon generator beta. Doors (`func_door`, `func_button`, `func_plat`) are excluded. All room connections use open arches with no moving geometry, trigger/target wiring, or runtime behavior. | active | bsp-dungeon architecture | BSP Dungeon Contract Evidence Phase 09 | 2026-07-24 | Doors add 11 evidence cells, inline brush model compilation complexity, trigger/target graph wiring, collision hull decomposition, and app-owned behavior adapter requirements. Open arches provide continuous walkable navigation with zero additional complexity. | Supporting doors would add 11 blocking evidence cells and require behavior adapter validation before generator output can be accepted. Future door support is not precluded — the connection model can gain an optional door flag. | Future door support requires the 11 door evidence cells to pass first. Until then, open arches are the only connection type. | `bsp-dungeon-generation.md`, `bsp-acceptance.md` | `bsp-beta.md` | 2026-08-24 |
| DECISION-20260724-04 | Player movement uses point-trace against compiler-preexpanded hull 1 clipnodes. The generator produces corridors with clear width ≥ 64 Quake units and clear headroom ≥ 80 Quake units based on the symmetric player hull (32×32×48). | active | bsp-dungeon navigation | BSP Dungeon Contract Evidence Phase 09 | 2026-07-24 | The ericw-tools `qbsp` compiler pre-expands hull 1 from the player's point-sized origin. Point traces against the expanded hull 1 clipnodes are the Quake-engine-compatible movement strategy. Corridor minimums are based on the symmetric hull (±(16,16,24)) which produces 32×32×48 bounding box. | Box tracing with an explicit player AABB would require the actual compiler-output hull extents (which differ from both FGD and symmetric records). Using FGD-documented asymmetric hull would produce 32×32×56 corridors. | The FGD vs symmetric hull dispute is unresolved (NAV-HULL-THRESHOLD NOT-RUN). The corridor minimums are conservative — both interpretations fit within 64×80. If Phase 06 threshold tests disprove point-trace compatibility, corridor minimums may need revision. | `bsp-dungeon-generation.md`, `bsp-spatial-physics.md` | `bsp-beta.md` | 2026-08-24 |
| DECISION-20260724-05 | M1/M2 bounds, construction parameters, output ceilings, and support corpus are frozen. They may not be tuned in response to downstream generator results. If evidence disproves any value, record a no-go rather than adjusting the value. | active | bsp-dungeon architecture | BSP Dungeon Contract Evidence Phase 09 | 2026-07-24 | Freezing the input domain before the generator exists prevents post-hoc tuning to make a failing configuration pass. The support corpus and ceilings are a contract the generator must meet, not a suggestion the generator can redefine. | Declaring ranges or tunable bounds would allow the generator to silently select a passing configuration and claim success. A fixed frozen domain forces honest pass/fail evidence. | Values may only change through explicit owner re-review with corresponding spec/changelog updates. The 2026-07-24 user-authorized portal repair approved a 112-unit minimum room span and explicit 64×80 portal/64×64 junction-center clearance. The contradicted M1 static-batch ceiling was not changed; missing runtime-budget measurements and batch remediation are tracked by GitHub #57. | `bsp-dungeon-generation.md`, `bsp-acceptance.md` | `bsp-beta.md` | 2026-08-24 |
| DECISION-20260724-06 | Keep the 12-configuration support corpus (8 nominal seeds + 4 boundary configs) frozen. Every configuration must generate deterministically, compile warning-free, reload strictly, remain sealed, and pass room/entity/corridor/portal/junction spatial witnesses without fallback. | active | bsp-dungeon validation | BSP Dungeon Contract Evidence Phase 09; portal repair validation | 2026-07-24 | A pre-declared corpus prevents cherry-picking only passing seeds. The 8 nominal seeds exercise edge values in the u64 space; the 4 boundary configs test map-class extremes. Compiled spatial witnesses catch additive-brush topology defects that intent-only reachability cannot. | A smaller corpus could miss edge cases; allowing skip/fallback behavior would let the generator silently exit its frozen domain. Renderer batch, timing, and memory measurements remain outside the current pure compiler harness. | All 12 entries passed the expanded repair corpus on 2026-07-24. Face/entity ceilings pass; static-batch compliance remains GitHub #57. | `bsp-dungeon-generation.md`, `bsp-acceptance.md` | `bsp-beta.md` | 2026-08-24 |
| DECISION-20260724-07 | Deterministic RNG framing: domain separator `"dungeon-gen/v1"` (UTF-8, prefixed), seed bytes in little-endian `u64`, SHA-256 per semantic stage. Four frozen tags: `room-placement`, `corridor-routing`, `entity-placement`, `light-placement`. Renaming or reframing a tag is an output-version change. | active | bsp-dungeon architecture | BSP Dungeon Generator Phase 01 contract reconciliation | 2026-07-24 | Named domain-separated streams make each stage's randomness independently attributable and keep insertion/reordering independent. SHA-256 framing produces stable 32-byte digests consumed as little-endian u64 streams. Tag spelling is part of output versioning, so renaming a tag changes generator output. | A single sequential RNG is simpler but couples every stage — adding a draw perturbs all later stages. Hierarchical nonce streams retain hidden consumption-order coupling. Named streams add hashing cost but make stage insertion safe and replayable. | Applies to the dungeon generator only (v1). The pattern follows DECISION-20260722-01 for voxel v2 but with dungeon-specific tags and domain separator. Future versions increment the domain separator version. | `bsp-dungeon-generation.md` | none | 2026-08-24 |
| DECISION-20260724-08 | Canonical `.map` serialization: entity order worldspawn-first then creation-index; key order alphabetical (ASCII byte); brush order by creation index; face order per brush: bottom, top, north, south, west, east; integer formatting decimal without scientific notation; Standard Quake texture mapping grammar as separately frozen by `DECISION-20260726-01`; line endings `\n`; exactly one terminal newline. Equivalent logical output must serialize identically. | active | bsp-dungeon architecture | BSP Dungeon Generator Phase 01 contract reconciliation; grammar clause reconciled by owner decision | 2026-07-24 | Deterministic byte-identical `.map` output requires a frozen serialization grammar. Without canonical ordering rules, semantically equivalent output could produce different bytes, breaking the determinism contract and making hash-based comparison unreliable. | A looser serialization contract (any valid `.map`, compared structurally) would require a semantic `.map` diff tool. Byte-identical comparison is simpler, faster, and less error-prone. Valve 220 was rejected by `DECISION-20260726-01`. | The canonical ordering must be applied at serialization time. Generator internal representations may use any ordering; only the emitted text must conform. Changing ordering or the approved Standard Quake grammar is an output-version change and requires owner re-review. | `bsp-dungeon-generation.md` | none | 2026-08-24 |
| DECISION-20260724-09 | CC0 procedural stone theme for dungeon generator beta. The delivered beta theme is project-authored, CC0-licensed, and contains a WAD2 archive, project palette, four visible base textures with exact-dimension normal/gloss PNG companions, and a compiler-only `skip` miptex. KB3D content is excluded. | active | bsp-dungeon architecture | BSP Dungeon Generator Phase 01 contract reconciliation; portal repair | 2026-07-24 | A project-authored CC0 theme satisfies the licensing gate without third-party clearance. The `skip` entry ensures compiler-helper faces never trigger a missing-texture warning. | Using KB3D content would require external conversion-rights clearance. Giving `skip` PBR companions would incorrectly expose it as a visible material. | Theme assets are deterministically generated by `build.py`; the generator references texture names and does not generate PNG companions during dungeon generation. | `bsp-dungeon-generation.md`, `bsp-renderer-lighting.md` | none | 2026-08-24 |
| DECISION-20260725-16 | Emit explicit role-bound room shells and a corridor-only 16-unit open-space union. Each room owns one floor, one ceiling, and four raster-split full-height wall masks; routed portal and endpoint-footprint cells are omitted from those masks. Corridor slabs and boundary walls derive from their own union so 64×64 turns remain clear without merging room slabs across the scene. | active | bsp-dungeon architecture | user-directed completion of BSP dungeon rendering repair | 2026-07-25 | The complete room/corridor union produced broad slabs with exposed side faces and obscured room-specific material/height roles. Fully independent shells would reintroduce additive portal and junction obstructions. The hybrid keeps room roles auditable while retaining union semantics only where corridor intersections need them. | A complete floor-plan union is simpler for compiled openness but hides room construction roles; independent corridor shells are simpler source geometry but obstruct L/T/X centers. Raster wall masks add source pieces, mitigated by deterministic rectangle merging. | Corridor footprints adjacent to endpoint-room corners can intersect wall cells without a normal centerline crossing; those cells must also be omitted to preserve the frozen 64×64 compiled junction witness. Compiler and MCP evidence remain mandatory. | `bsp-dungeon-generation.md`, `bsp-acceptance.md` | `bsp-generator-compiled-spatial-validation.md` | 2026-08-25 |
| DECISION-20260726-01 | **APPROVED (source: `owner-sprint-20260726`) — Serializer grammar is Option A, Standard Quake offset/rotation/scale.** Do not implement Valve 220. Remove dead `BrushFace.u_axis`/`v_axis` fields and align specifications to the actual Standard grammar. | active — grammar retained; scale clause superseded by `DECISION-20260727-02` | bsp-dungeon | `owner-sprint-20260726` | 2026-07-26 | Standard Quake bytes/tests are compiler-compatible; dead identical/collinear axis placeholders are never serialized. | **Rejected: Valve 220.** It would require valid non-collinear per-face U/V axes, bracket serialization, changed map hashes, regenerated corpus entries, and compiler/lightmap/visual revalidation. | The original scale-`1.0` default is superseded; canonical scale is now `0.25`. Grammar and dead-field decisions remain active. | `bsp-dungeon-generation.md` §§13,16; `bsp-compatibility.md` §12.3 | `quake-qbsp-procedural-dungeon-research.md` | Any later grammar, coordinate-default, or public-IR change requires owner re-review and output-version/corpus revalidation. |
| DECISION-20260726-02 | **APPROVED (source: `owner-sprint-20260726`) — Vertical corridor IR is Option A, fixed at 80.** Public `Corridor.height` must equal 80 and reject non-80 input; generated routes always use 80. `build_corridor_slabs` and `build_corridor_boundary_walls` must derive from `corridor.height`, not a global-height reconstruction. | active — APPROVED | bsp-dungeon | `owner-sprint-20260726` | 2026-07-26 | Every generated route already uses 80 and the frozen construction contract requires 80-unit clear headroom. A fixed public invariant removes the exposed inconsistent non-80 path without expanding beta geometry/sealing behavior. | **Rejected: variable public heights.** It would need quantized constructibility rules, field-consistent routing/emission, non-80 tests, and renewed compiled spatial evidence. | Phase 01 records this contract only: current source still accepts non-80 public values and reconstructs slabs/walls through `CORRIDOR_HEIGHT` until the authorized generator phase implements the invariant. | `bsp-dungeon-generation.md` §§4,7,11,16; `bsp-compatibility.md` §12.4 | `quake-qbsp-procedural-dungeon-research.md` | Before Phase 09 implementation; any variable-height request, non-80 proposal, or public-IR boundary change requires owner re-review and renewed compiled spatial evidence. |
| DECISION-20260726-03 | **Compatibility-family vs exact-publication-profile distinction.** Two profile names are distinct concepts: `q1-portable-ericw` is the compatibility family (compiler dialect); `ericw-q1-bsp2-generated` is the exact publication profile (checked-in TOML with pinned args). They are not interchangeable aliases. Both names retained without renaming. Exact baseline evidence is recorded at `.internal-dev/captures/bsp-dungeon-repair-baseline/manifest.json` (baseline ID `5fda7dae1d1f3da51c064d1d136418dae9c0e79a43ad73396a496bba81270c35`; exact BSP SHA-256 `b24922904beccd06617b47715243993d8f40b4f262b4140200a69a3bdd6326d7`). | active | bsp-dungeon | Phase 01 baseline freeze | 2026-07-26 | Two concepts serve different roles: family for dialect classification, exact profile for pinned args and provenance. Collapsing them would lose the distinction and risk profile-arg drift. | One canonical name would simplify documentation but risk conflating the family concept with a specific profile file. | Existing spec uses `q1-portable-ericw` for the family; profile file is named `ericw-q1-bsp2-generated`. | `bsp-dungeon-generation.md`, `bsp-compatibility.md` | `quake-qbsp-procedural-dungeon-research.md` | 2026-08-26 |
| DECISION-20260727-01 | **Legacy BSP lightmap transfer uses normalized UNORM modulation, style weighting, and 2× overbright before albedo multiplication.** Do not apply `pow(2.2)` or Lambertian `/ PI`; a missing-lightmap face uses neutral modulation. This applies consistently to lightmapped, PBR, and liquid BSP shaders. | active — owner-approved repair | bsp-beta renderer | user-directed end-to-end BSP texture rendering repair; vkQuake source comparison | 2026-07-27 | WAD mip-0 and BSP-embedded 1024² bytes are intact, and the current generated artifact contains 903 valid lightmapped faces. The prior physical decode reduced representative light bytes twice and caused a dim, low-contrast image. vkQuake's world path modulates normalized texture/light bytes with classic overbright; the repaired seed-0 capture doubles mean brightness and restores visible stone/mortar variance without an arbitrary albedo gain. | **Rejected:** retain physical irradiance decode (`pow(2.2)` plus `/ PI`), because it contradicts the legacy byte calibration and observed output. **Rejected:** multiply albedo by 3×, because it masks only symptoms, distorts fallback/PBR parity, and is not source-format semantics. | Formal same-camera vkQuake SSIM remains pending. This decision governs legacy BSP baked-light bytes, not general mesh PBR or environment-map transfer. | `bsp-renderer-lighting.md`, `bsp-acceptance.md`; `docs/guide/18-bsp-beta.md`; `docs/internal/18-bsp-runtime-and-lifetime.md` | `.internal-dev/captures/bsp-beta/headless-1480388/`; upstream vkQuake `Shaders/world.frag` / `world_common.inc` | Any transfer, overbright, fallback, or output-tone-map change requires owner re-review and fixed-camera comparison. |
| DECISION-20260727-02 | **Keep Standard Quake mapping grammar and set generated-dungeon canonical texture scale to `0.25 0.25`.** | active — owner-approved repair; supersedes only the scale-`1.0` clause of `DECISION-20260726-01` | bsp-dungeon renderer-facing authoring | user-directed end-to-end BSP texture rendering repair | 2026-07-27 | Scale `1.0` maps only 64 texels across a 64-unit span of a 1024² source and produced oversized, soft detail. Scale `0.25` maps 256 source texels across that span while retaining compiler-compatible Standard Quake syntax. Fresh seed-0 output remains within M1 ceilings (1,124 faces, 6 submitted batches). | **Rejected:** renderer-only UV multipliers, because they diverge authored/compiler texinfo semantics and do not invalidate baked-light extents. **Rejected:** retain `1.0`, because fixed-camera evidence shows insufficient source-detail density. | Scale changes affect compiler subdivision and lightmap extents; all-12 corpus regeneration is required before complete current-corpus claims. | `bsp-dungeon-generation.md`, `bsp-acceptance.md` | `.internal-dev/captures/bsp-beta/headless-1459408/`, `.internal-dev/captures/bsp-beta/headless-1480388/` | Any default-scale change requires owner re-review plus compiler, ceiling, lightmap, and visual revalidation. |
| DECISION-20260727-03 | **Generated-dungeon acceptance cameras use authored-height non-solid semantic origins and deterministic compiled-contents clearance probes.** Do not add an eye-height offset to `info_player_start`; orient toward the cardinal direction with greatest clear distance. | active — owner-approved repair | bsp-beta acceptance | user-directed iterative headless-capture repair | 2026-07-27 | The generated spawn origin is already at eye height. Adding 2 m put the camera above the 80-unit corridor interior, while approximate map-center coordinates could be solid; incorrect Quake-to-engine yaw conversion could then face a wall. Compiled point-contents and deterministic cardinal probes make the fixed camera recognizable and reproducible. | **Rejected:** static approximate XYZ/yaw tables, because generator topology varies by seed and those values were not validated against compiled contents. **Rejected:** unconditional vertical offset, because authored entity semantics already provide the correct eye. | Label-specific origin selection and the default-forward facing fallback remain deterministic if no semantic entity or clear cardinal ray is available. Each selected origin is checked for non-solid contents. | `bsp-acceptance.md` | `.internal-dev/captures/bsp-beta/headless-1480388/` | Camera-selection changes require camera unit tests and fixed-camera recapture. |
| DECISION-20260727-04 | **Dungeon explorer cache reuse requires a complete deterministic fingerprint and verified resource closure.** Fingerprint generator/compiler source, Cargo lock state, profile, WAD, palette, and selected compiler executables; verify exact BSP, LIT, WAD, palette, and generator identities before reuse. | active | bsp-dungeon tooling | user-directed stale-artifact repair | 2026-07-27 | A curated source list omitted `serialize.rs`, so a scale change could reuse stale compiled texinfo. The old verifier also matched both `generator` and `generator_version` and failed to validate LIT/WAD hashes. Hashing the full producer inputs and checking every cached artifact prevents stale rendering evidence and makes genuine cache hits observable. | **Rejected:** manual cache busting, because it is non-deterministic and easy to omit. **Rejected:** a curated serializer file list, because future output-affecting modules can be missed again. | The cache is a development accelerator, not publication provenance. Changes to the self-hashed explorer script intentionally invalidate it. | `bsp-dungeon-generation.md`, `bsp-acceptance.md` | `tools/dungeon_explore.sh`; `.internal-dev/captures/bsp-dungeon-generator/m1-seed-0.manifest.toml` | Re-review when compiler discovery, profile inputs, or cache manifest schema changes. |
| DECISION-20260728-01 | Unforgeable ObjectId with provenance-bound SceneRuntimeId. All scene objects (nodes, point lights, directional lights, spot lights) share one unified `ObjectId` type with private fields, per-scene provenance, and no serialization. `SceneObjectId` remains the durable identity for persistence. | active | object-system | Scene object system sprint Phase 02 | 2026-07-28 | Cross-scene identity forgery is impossible because `SceneRuntimeId` comparison rejects IDs from different scene instances. Private fields + `pub(crate)` construction prevents callers from synthesizing IDs. The dual-identity split (`ObjectId` for runtime, `SceneObjectId` for persistence) prevents accidental serialization of transient state. | A single numeric ID would be simpler but would allow cross-scene forgery and accidental serialization. Using `SceneObjectId` for runtime lookups would conflate persistent and transient identities. | `ObjectId` must never be persisted. Commands anchor redo on `SceneObjectId`, not `ObjectId`. | `scene-object-system.md`, `architecture.md`, `api.md` | none | 2026-08-28 |
| DECISION-20260728-02 | Prepare/commit lifecycle for all object mutations. Every mutation validates and reserves in a prepare step, then applies atomically in commit. Failed prepare leaves the world unchanged. Commit is non-fallible after successful prepare. | active | object-system | Scene object system sprint Phase 06 | 2026-07-28 | Two-phase mutation prevents partial state: validation failures cannot leave half-created objects, slot exhaustion is caught before reservation, and the reverse index is never partially updated. Commands build on the same prepare/commit kernel for undo/redo atomicity. | Immediate inline mutation would be simpler per-call but would require rollback logic in every mutation path and make undo/redo harder to implement correctly. | Prepare plans are typed (CreateNodePlan, RemoveNodePlan, etc.) and must not escape the mutation scope. | `scene-object-system.md`, `architecture.md`, `api.md` | none | 2026-08-28 |
| DECISION-20260728-03 | The four object kinds — Node, PointLight, DirectionalLight, SpotLight — remain unchanged. No new public `ObjectKind` variants are admitted in this implementation. The exhaustive four-kind contract is frozen. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | The current four-kind ontology cleanly partitions scene objects: spatial containers (Node), positional emitters (PointLight, SpotLight), and global directional emitters (DirectionalLight). Adding a fifth kind would require new GPU submission paths, new serialization types, new capabilities, new transform encoding, and new event vocabulary before any gameplay value is proven. | A generic "custom object" kind or dynamic kind registry would allow arbitrary extensions but would break the exhaustive-match safety of the current system and require type-erased APIs across renderer, physics, and scripting boundaries. | Kind additions require a specification revision and owner re-review. The `ObjectKind` enum in `engine_events` is the canonical source; adding a variant must be accompanied by updates to `ObjectCapabilities::for_kind`, `ObjectTransform::canonical_for_kind`, `SerializedScene`, `ObjectRecord`, and all exhaustive-match sites. | `scene-object-system.md`, `api.md`, `architecture.md`, `src/events/src/lib.rs` | none | 2026-08-28 |
| DECISION-20260728-04 | Node plus versioned components represents physics, 2D, audio roles, and LookAt without adding new object kinds. The existing `Node` kind carries typed `ComponentEnvelope` attachments through the component document model; physics bodies, 2D transforms, audio sources, and camera look-at targets are expressed as components on a Node rather than as separate object kinds. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | The component system (`OBJ-20260728-13`) already supports typed, versioned, multi-instance attachments with property reflection. Expressing domain roles as components preserves the four-kind ontology while providing discoverable, type-checked, serializable, and undoable domain data without expanding the object-kind enum. | Adding a PhysicsBody, Sprite2D, AudioSource, or LookAtTarget object kind for each role would fragment the object system, require parallel slot stores, and complicate parenting/grouping. Using a single generic "Entity" kind with a type-tag field would lose compile-time kind discrimination. | Component keys are lowercased dot-qualified names (e.g., `physics.rigid_body`, `renderer.look_at`, `audio.spatial_source`). The component registry is caller-populated; engine crates define their own component types. Components carry only durable references — `SceneObjectId`, `AssetId`, `ComponentInstanceId` — never runtime ObjectId, slot indices, or GPU handles. | `scene-object-system.md`, `api.md`, `architecture.md` | none | 2026-08-28 |
| DECISION-20260728-05 | Scene format version remains 2. `SCENE_FORMAT_VERSION == 2` is the canonical persistence version. v1 loads through forward-compatible migration; v3 is not authorized until an evidence-backed requirement demonstrates v2 insufficiency. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | v2 already supports all four object kinds, typed lights, collision metadata, audio references, visibility, prefab metadata, shadow-owner configuration, components, and editor metadata. No known persistence gap requires a format version bump. | Incrementing the format version preemptively would create unnecessary migration paths and break existing v2 fixture compatibility. | A future v3 must include a migration path from v2 and must be justified by a specification change that v2 cannot represent. The `SCENE_FORMAT_VERSION` constant in `src/renderer/src/api/scene.rs` is the single source of truth. | `api.md`, `scene-object-system.md` | none | 2026-08-28 |
| DECISION-20260728-06 | Bridges stay app-owned and mapped worlds are exclusively mutated through the bridge. The renderer exports neutral DTOs (mesh geometry, scene bounds, transforms); the app bridge owns physics world creation, collider recipes, audio source placement, and behavior wiring. No renderer-to-physics, renderer-to-audio, or renderer-to-scripting dependency edges exist. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | This preserves `DECISION-20260725-06` (neutral DTO before bridge crate) and `DECISION-20260723-06` (app-owned physics and behavior bridges). Exclusively mutating mapped worlds through the bridge prevents the renderer from silently creating physics objects, audio sources, or script contexts during scene loading or frame submission. | Putting collider creation inside the renderer would invert the renderer-to-physics dependency. Letting the renderer directly mutate a physics world would couple frame submission to physics step ordering. | The bridge remains app-local until a second consumer is confirmed. The BSP coordinator and dungeon dogfood bridge are the current proofs. | `architecture.md`, `service-graph.md`, `services.md` | none | 2026-08-28 |
| DECISION-20260728-07 | Debug lines and debug sprites use independent default-off Cargo features (`debug-lines`, `debug-sprites`). Neither feature is in `default`. When disabled, no debug-draw shaders, pipelines, descriptor layouts, or frame-path branches are compiled. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | Following `DECISION-20260725-07` (feature flags for new rendering paths), debug visualization is an opt-in diagnostic capability that must not affect default-build behavior, binary size, shader compilation, or frame-path performance. | Shipping debug drawing unconditionally would add shader variants, pipeline objects, and frame-path branches to every build. A runtime-only flag would not prevent link-time coupling. | Features are exactly `debug-lines` and `debug-sprites`. Both excluded from `default`. Remove a flag only after its complete evidence branch passes. | `architecture.md`, `api.md`, `deferred-features.md` | none | 2026-08-28 |
| DECISION-20260728-08 | Pure 2D and non-depth-comparable overlay are admitted as deferred capabilities. Hybrid depth (2D sprites depth-tested against the 3D scene) is excluded. 2D rendering uses a dedicated overlay pass with no depth attachment; overlay content is never depth-tested against 3D geometry. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | A dedicated overlay pass is simple, deterministic, and does not require per-sprite depth values or depth-buffer interleaving. Hybrid depth would require 2D sprites to carry Z values, participate in early-Z, and interact with 3D alpha blending — a substantial renderer complexity increase with no demonstrated gameplay need. | Allowing depth-testable 2D would require a per-sprite depth policy, depth-buffer layout changes, and pipeline variants. Supporting only pure overlay keeps the 2D path narrow and the 3D depth buffer unmodified. | Pure 2D remains deferred until `debug-sprites` or a UI/map-overlay consumer proves the need. The exclusion of hybrid depth is permanent unless an owner-approved specification revision demonstrates a required use case and pass ordering design. | `deferred-features.md`, `architecture.md` | none | 2026-08-28 |
| DECISION-20260728-09 | Rapier2D and spatial audio are conditional clean-admit/clean-exclude branches. Rapier2D is admitted if and only if a 2D physics consumer is confirmed and the existing 3D Rapier integration pattern (app-owned bridge, neutral DTOs, no renderer dependency) is followed. Spatial audio is admitted if and only if a spatial audio consumer is confirmed and the component-based audio-source pattern is proven. Until then, both are clean-excluded: no dependency, no crate, no feature flag, no stub. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | Conditional admission prevents premature crate dependencies and API surface while keeping the architectural path clear. The clean-exclude state means zero code, zero Cargo.toml entries, and zero feature flags — not a disabled feature. | Adding Rapier2D or a spatial audio crate now would add maintenance burden without a consumer. Pre-creating feature flags would suggest readiness that does not exist. | Admission requires: a named consumer app, an accepted plan with bridge design, and owner approval. Until all three are present, these remain clean-excluded. | `deferred-features.md`, `architecture.md` | none | 2026-08-28 |
| DECISION-20260728-10 | Billboard, Animator, SpawnSpec, analog input, automatic registration, and new public kinds are excluded from this implementation. None of these capabilities are admitted, deferred, or feature-gated — they are explicitly excluded and must pass through the specification admission process before any code is written. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | Each excluded capability either lacks a consumer (Billboard, Animator, SpawnSpec), lacks an input-model design (analog input), or violates the frozen four-kind ontology (automatic registration of new kinds, new public ObjectKind variants). Explicit exclusion prevents scope creep and accidental partial implementation. | Deferring these would suggest they are accepted future work, which they are not. Feature-gating them would create dead feature flags. | Admission of any excluded capability requires: a named consumer or use case, an accepted specification revision, and owner approval. Automatic registration and new public kinds require re-opening `DECISION-20260728-03`. | `deferred-features.md` | none | 2026-08-28 |
| DECISION-20260728-11 | Canonical components contain durable references only. Component data (the `data: serde_json::Value` payload of a `ComponentEnvelope`) must reference only `SceneObjectId`, `AssetId`, `ComponentInstanceId`, or other string-based durable IDs. Runtime `ObjectId`, slot indices, generation counters, Rapier handles, playback handles, and GPU descriptor references are forbidden in component data. Fixed-tick owner order is: input → physics → audio → scripting, each advancing exactly one fixed step per app frame when enabled. | active | game-primitives | Game Engine Primitives Phase 01 | 2026-07-28 | Durable references ensure components survive serialization round-trips and scene reloads without dangling pointers or stale handles. The fixed-tick order prevents non-deterministic physics/audio/scripting interaction and makes each subsystem's step independently auditable. | Allowing runtime handles in components would require handle remapping on load, handle invalidation tracking, and fence-aware retirement for every component field — scope far beyond current needs. Allowing subsystems to run at independent rates would create complex inter-system dependency graphs. | The fixed-tick order is a specification contract, not yet enforced by the runtime. Subsystems that are not enabled (no physics world, no audio context) are skipped without advancing their clock. | `scene-object-system.md`, `architecture.md` | none | 2026-08-28 |
| DECISION-20260728-12 | Rapier2D is delivered behind the `rapier2d` Cargo feature (default-off) in the `physics` crate. `PhysicsWorld2D` mirrors the 3D `PhysicsWorld` API with 2D-specific body/collider types, ray casts, overlap queries, contact records, and transactional registration. The conditional admission contract from `DECISION-20260728-09` is satisfied: a 2D physics consumer is confirmed (Game Engine Primitives Phase 08), the 3D Rapier integration pattern is followed (app-owned bridge, neutral DTOs, no renderer dependency), and 42 integration tests validate the full lifecycle. | active | game-primitives | Game Engine Primitives Phase 08 | 2026-07-28 | The conditional clean-admit branch required a named consumer and an accepted bridge design before any code. Both conditions are met. | Adding Rapier2D without a consumer would have been premature. Pre-creating feature flags before the admission contract was satisfied would have suggested readiness that did not exist. | Rapier2D is delivered as opt-in. Default builds are zero-impact. Future scene bridge or character controller 2D work is deferred until a concrete consumer requires them. | `api.md`, `architecture.md`, `deferred-features.md` | none | 2026-08-28 |
| DECISION-20260728-13 | Spatial audio is delivered behind the `spatial-audio` Cargo feature (default-off) in the `audio` crate. Stereo panning uses equal-power law, distance attenuation uses inverse-square clamping, and the mono-to-stereo source adapter uses atomic gain control. `AudioSource` and `AudioListener` component DTOs are provided. The conditional admission contract from `DECISION-20260728-09` is satisfied: a spatial audio consumer is confirmed (Game Engine Primitives Phase 09), the component-based audio-source pattern is proven, and 20 integration tests validate spatialization, blend, device-backed playback, and atomic thread safety. | active | game-primitives | Game Engine Primitives Phase 09 | 2026-07-28 | The conditional clean-admit branch required a named consumer and a proven component-based audio-source pattern. Both conditions are met. | Adding spatial audio without a consumer or proven pattern would have been premature. | Spatial audio is delivered as opt-in with sample-level admission proof. Listener choice, scene synchronization, source lifecycle, and event policy remain caller/app-owned. No HRTF, Doppler, or occlusion. | `api.md`, `architecture.md`, `deferred-features.md` | none | 2026-08-28 |
| DECISION-20260729-01 | **Enhanced v2 RNG domain isolation.** Enhanced v2 uses domain separator `"dungeon-gen/v2"` with six frozen stage tags (`layer-placement`, `vertical-topology`, `vertical-routing`, `theme-assignment`, `feature-placement`, `corridor-variance`), fully independent from Legacy v1's `"dungeon-gen/v1"` with its four tags. Same master seed values produce different output across profiles. Renaming or reframing a tag in either domain is an output-version change for that profile only. | active | bsp-dungeon | Enhanced v2 implementation Phases 02–03 | 2026-07-29 | Named domain-separated streams with profile-versioned prefixes prevent cross-profile RNG coupling. The independent `v2` domain ensures Legacy v1 byte-identical corpus output is preserved regardless of Enhanced v2 stage changes. | A single shared domain with profile-specific stage tags would be simpler but would couple output versioning across profiles. A master-seed-based branching scheme would require sequential consumption ordering. | Both domains use identical SHA-256-chained `StageRng` internals; only the domain prefix and tag set differ. | `bsp-dungeon-generation.md` | none | 2026-09-29 |
| DECISION-20260731-01 | **Enhanced v3 architectural proof completed — GO.** A private, test-only proof package demonstrated cardinal/45° chamfered-octagonal geometry, pointed-arch portal apertures, grounded assemblies, and dense M2-budget compositions. Evidence: 36/37 rows PASS, 1 NOT_RUN (ericw-tools tool limitation on small maps), 0 FAIL. No production code, `GenerationProfile` variant, or living specification was changed. The proof is accepted as architectural feasibility evidence; it is not a production authorization. Deferred capabilities (diagonal portals, concave rooms, accessible upper features, segmented-arch integration, additional room families, trim theme role, corridor/stairwell treatment, third navigation layer, non-worldspawn entities) remain deferred per G-12. See `.internal-dev/plans/enhanced-v3-proof/decision-package.md`. | active | bsp-dungeon | Enhanced v3 architectural proof (Phase 10 closeout) | 2026-07-31 | The proof satisfied every owner-approved gate with compiled spatial evidence, budget safety margins, compatibility freeze preservation, and live GPU startup validation. Known gaps are tool limitations (ericw-tools small-map segfault) and deferred capabilities — not proof failures. | Rejecting the proof would discard 36 PASS evidence rows and the compiled spatial/visual/live-startup evidence collected across 10 phases. Promoting deferred capabilities to integration would violate the G-12 contract. | This row remains the historical proof-boundary decision; `DECISION-20260731-05` later supersedes only its segmented-integration disposition. The private proof itself remains intact. No `EnhancedV3` variant existed in `GenerationProfile` at proof time, and the `enhanced-v3` proof tag remains unrecognized in production dispatch. | `bsp-dungeon-generation.md` | `bsp-enhanced-v3-proof.md` | 2026-09-30 |
| DECISION-20260731-02 | **EnhancedV3 production profile authorized.** `EnhancedV3` is added to `GenerationProfile` with production tag `"m3"`. RNG domain `"dungeon-gen/v3"` with four frozen stage tags: `v3-placement`, `v3-topology`, `v3-features`, `v3-detail`. Production module at `src/bsp_generator/src/enhanced_v3/`. Two-layer M2 arrangement (floor Z=0, upper Z=192, height 176) with cardinal + 45° geometry only. Chamfered/octagonal rooms, pointed-arch portal apertures, grounded assemblies. Sparse/Moderate/Rich presets with minimum-identity requirements and typed errors. cc0_dungeon_v2 theme reused without modification. 12+ entry v3 corpus with deterministic replay, warning-free BSP2 compilation, and strict reload. v1/v2 frozen corpora remain byte-identical. Phase 01 records these contracts as intended truth; production code changes begin in subsequent phases. See `.internal-dev/reviews/enhanced-v3-bridge-reconciliation.md`. | active | bsp-dungeon | EnhancedV3 Production Sprint Phase 01 | 2026-07-31 | The architectural proof (36 PASS, 1 NOT_RUN, 0 FAIL) provides compiled spatial, budget, compatibility, and live-GPU evidence for every authorized capability. Owner disposition is GO. Production sprint must deliver `dungeon_gen --class m3`, `engine_pack enhanced-dungeon-v3`, and `dungeon_explore.sh --class m3`. | Rejecting production authorization would leave the proof evidence unused. Authorizing capabilities beyond the proof boundary (diagonal portals, concave rooms, etc.) would violate G-12. | Production code changes are not authorized in Phase 01 (documentation only). The `enhanced-v3` tag from the proof remains unrecognized in production dispatch — the production tag is `"m3"`. All deferred capabilities remain deferred. | `bsp-dungeon-generation.md`, `bsp-compatibility.md`, `bsp-acceptance.md`, `bsp-transaction-ownership.md`, `api.md`, `architecture.md`, `.internal-dev/reviews/enhanced-v3-bridge-reconciliation.md` | `bsp-enhanced-v3-proof.md` | 2026-09-30 |
| DECISION-20260731-03 | **EnhancedV3 RNG domain and frozen stage tags.** v3 uses domain separator `"dungeon-gen/v3"` with SHA-256 framing and four frozen stage tags: `v3-placement` (two-layer room placement with 45° footprint support), `v3-topology` (topology and transition selection), `v3-features` (chamfered footprints, pointed arches, grounded assemblies), `v3-detail` (preset-driven feature density, pillar placement). Fully independent from v1's `"dungeon-gen/v1"` and v2's `"dungeon-gen/v2"`. Same master seed values produce different output across all three domains. Renaming or reframing a tag is an output-version change for the v3 profile only. | active | bsp-dungeon | EnhancedV3 Production Sprint Phase 01 | 2026-07-31 | Named domain-separated streams with profile-versioned prefixes prevent cross-profile RNG coupling. The four tags correspond to the four semantic stages proven in the architectural proof. Both v1 (4 tags) and v2 (6 tags) domains remain frozen and independent. | A single shared domain or master-seed-based branching would couple output versioning across profiles. Fewer tags would conflate semantically distinct stages; more tags would add RNG derivation cost without proportional benefit. | Both v1 and v2 domains use identical SHA-256-chained `StageRng` internals. The v3 domain follows the same pattern with its own prefix and tag set. | `bsp-dungeon-generation.md` | `bsp-enhanced-v3-proof.md` | 2026-09-30 |
| DECISION-20260729-02 | **Enhanced v2 vertical contract selected.** Evidence-selected constants from Phase 01 vertical_feasibility test: lower_floor_z=0, upper_floor_z=192, room_height=176 (both layers), riser=16, tread=16. Total Z span for two layers: 192+176=368 ≤ 384. All four candidate fixtures pass warning-free BSP2 compilation, strict load, spatial witnesses (room interiors, portals, stair treads, landings, headroom, solid shells), deterministic replay, and the frozen 12-entry legacy corpus remains byte-identical. | active | bsp-dungeon | Phase 01 vertical feasibility evidence | 2026-07-29 | Generated evidence from `vertical_feasibility.rs` proves two-layer sealed stair-connected dungeons within the unchanged M2 Z envelope. 16-unit tread selected as the first passing candidate in ascending order; 32-unit tread also passes all gates and remains a supported option. | **Rejected: no candidate passes.** All four tested candidates pass all gates. | Enhanced v2 implementation phases (02-10) are now unblocked. The selected tread depth of 16 units is the default; 32-unit tread is a supported alternative. Both require live fixed-step movement evidence per Phase 09. | `bsp-dungeon-generation.md` | none | 2026-09-29 |
| DECISION-20260731-04 | **EnhancedV3 production sprint closeout — GO.** The EnhancedV3 production profile is delivered: `GenerationProfile::EnhancedV3` with tag `"m3"`, `generate_v3()` entry point, `V3Config` with `V3Preset::Sparse/Moderate/Rich`, `V3Error` (45 typed variants), `EnhancedV3Metadata` (22 accessors), 15 source modules in `src/enhanced_v3/`, i128 Rational geometry, cardinal+45° approved normals, grounded assembly support graph, pointed-arch portal apertures, 6 grammar families, minimum-identity enforcement, theme reuse of cc0_dungeon_v2. Tooling: `engine_pack enhanced-dungeon-v3`, `dungeon_explore.sh --class m3`. Evidence: 25 contract baseline tests pass, 36/37 proof rows PASS, v1/v2 24-entry corpora byte-identical, dense M2 budget at 2,404 faces/6 entities/4 batches (well within ceilings), live GPU startup 21,574 frames/0 errors. Deferred capabilities (diagonal portals, concave rooms, accessible upper features, segmented-arch integration, trim theme, lattice-slope walls) remain deferred per G-12. Closeout documents produced: updated AGENTS.md, guide §3, API reference §Enhanced v3, internal docs §17, finalized specification §20, knowledge file, changelog, and final review. | active | bsp-dungeon | EnhancedV3 Production Sprint Phase 10 closeout | 2026-07-31 | The production sprint delivered every authorized capability: m3 profile dispatch, cardinal+45° geometry, pointed-arch portals, grounded assemblies, density presets, CLI tooling, and full documentation/closeout records. All evidence gates pass; v1/v2 baseline compatibility is preserved. | Rejecting closeout would require re-running already-passing evidence gates. Promoting deferred capabilities to production would violate the G-12 contract boundary. | This row records the original production closeout boundary; `DECISION-20260731-05` later supersedes only its segmented-integration disposition. The v3 production corpus (12+ entries) was NOT_STARTED at this closeout, and its face estimates were still proof-derived. | `bsp-dungeon-generation.md`, `bsp-compatibility.md`, `bsp-acceptance.md` | `bsp-enhanced-v3-production.md` | 2026-09-30 |
| DECISION-20260731-05 | **Integrate the cardinal segmented-arch explorer override without changing default EnhancedV3 output.** `ArchType::Segmented` remains accepted by `V3Config`, cycles between `Pointed` and `None`, and is exposed by `dungeon_gen --arch-type segmented` and full-config package generation. The historical two-band surround is retained; a one-quantum exterior cap backs its 32-unit Z=112–128 centre recess where the corridor roof ends at Z=112. The complete 64×80 navigable throat remains open. | active | bsp-dungeon | explicit user authorization for the EnhancedV3 explorer scope | 2026-07-31 | The accepted explorer contract already exposed `none`, `pointed`, and `segmented`. Rejecting segmented narrowed an authorized capability instead of repairing the additive-brush leak. A corridor-side backing cap closes the sole exterior path while preserving the visible segmented shape and compatibility throat. | Lowering or flattening the segmented crown would erase the requested surround; changing shared corridor or pointed geometry would risk default-byte drift; retaining rejection would violate the accepted scope. | This promotes only cardinal segmented surrounds through the explorer path. Pointed remains the default and its bytes are frozen. The old focused fixture remains valid historical proof, while diagonal portals, concave rooms, and other G-12 capabilities remain deferred. | `bsp-dungeon-generation.md`, `bsp-acceptance.md` | `bsp-enhanced-v3-production.md` | 2026-09-30 |
| DECISION-20260801-01 | **Use one app-owned, mode-exclusive M3 GUI and retain the existing two-domain BSP regeneration pipeline.** F1/F2 are global menu controls in windowed `bsp_beta --m3-generate`; keyboard and mouse modes never coexist and block the other device plus gameplay before normal routing. App-owned ImGui registration exists only while open. Complete validated GUI snapshots use the existing background engine_pack publication and coordinator mount transaction; Apply-and-Close is keyed to the exact latest request and is fulfilled only after commit. | active | bsp-beta / bsp-dungeon | user directive for production-ready EnhancedV3 in-game controls | 2026-08-01 | Device-exclusive modes and pre-routing suppression eliminate typing/click-through into camera/gameplay. Reusing the proven worker, package, coordinator, and retirement boundaries avoids a synchronous compiler path or a new transaction domain. Registering app UI only while open restores normal input immediately and permits a noncapturing title toast after Apply-and-Close. | Relying only on ImGui `want_capture` was rejected because capture bits are frame-delayed and do not express the requested device exclusivity. Two simultaneous menus were rejected because they recreate dual-input ambiguity. A synchronous event-thread compiler call was rejected because it stalls rendering and bypasses latest-wins publication. | Cursor focus events still route to renderer platform policy but never gameplay. Fixed wall thickness remains display-only. Source/helper tests and timeout-bound startup pass; automated F1/F2/click interaction and formal WSI lifecycle remain NOT_RUN. | `architecture.md`, `bsp-dungeon-generation.md`, `bsp-acceptance.md`, `bsp-transaction-ownership.md` | `bsp-beta-m3-gui-input.md` | Re-review when menu mode ownership, input-device suppression, app-UI registration lifetime, or regeneration transaction boundaries change. |
| DECISION-20260802-01 | **EnhancedV3 baseline-v3 corpus re-frozen at commit 8203a7f8.** The Phase 01 baseline freeze regenerated all 12 EnhancedV3 corpus entries (Sparse/Moderate/Rich × seeds 0/42/99/255) against the current task-base HEAD (`8203a7f8` — "fix(enhanced_v3): route overlap safety, guaranteed rectangles, multi-stair layouts"). All 12 cells compiled warning-free through the pinned ericw-tools 2.0.0-alpha3 BSP2 profile with dual-root byte-identical determinism (map, metadata, BSP, LIT), zero leaks, and strict budget compliance. The new manifest replaces the prior stale-hash manifest at `src/bsp_generator/tests/fixtures/enhanced_v3_corpus/manifest.json`. v1 (12-entry) and v2 (12-entry) frozen corpora remain byte-identical. | **APPROVED** — owner authorized via autonomous-delegation directive (session 2026-08-02: 'work autonomously through, never prompt, always continue') | bsp-dungeon | Phase 01 baseline freeze (`phase01_baseline_freeze_12_cells` test, 2026-08-02) | 2026-08-02 | Commit `8203a7f8` changed chamfered-room assignment (deterministic every-5th-room rectangle) and made the placement grid stair-aware, causing all 12 baseline-v3 map/metadata/BSP/LIT hashes to differ from the prior manifest. The prior manifest was from commit `71c507d8` (the stopgap before the repair). This freeze reconciles the oracle to task-base code without changing generator behavior. The manifest remains `enhanced-v3-corpus/v1` — not renamed to imply Richness. | Rejecting the freeze would leave stale hashes in the manifest, causing `corpus_entries_deterministic` to fail and blocking all downstream v3 qualification evidence. Selecting replacement seeds would change generator behavior instead of fixing the oracle. | Owner acceptance is required before the manifest is authoritative. The freeze provenance sidecar (`baseline-freeze-provenance.json`) records the task-base commit, pre-freeze dirty tree, compiler tool hashes, theme/palette SHA-256, exact command vectors, and per-cell evidence. The complete old-versus-proposed hash table is retained below for the owner decision. | `bsp-dungeon-generation.md`, `bsp-acceptance.md`, `bsp-compatibility.md`, `bsp-transaction-ownership.md` | none | 2026-09-02 |
| DECISION-20260802-02 | **EnhancedV3RichnessV1 contract freeze.** A separate immutable contract domain `dungeon-gen/v3-richness/v1` is added to the living BSP specifications. It freezes: exactly 30 shared semantic archetypes, 15 shared semantic props, 12 shared semantic lighting recipes, 3 themes, 4 cave cell types, 4 vertical opening types, same-XY composite reservation rules, theme invariance (theme cannot influence blueprint candidate keys or cave decisions), budget ceilings (< 500 entities, < 15,000 faces, < 100 lights, < 800 static batches), typed stable errors, and atomic public exposure gating. This freeze authorizes NO implementation code, CLI, GUI, or public surface. Baseline V3Config, tags, RNG domains, CLI, GUI, package manifests remain frozen. | **APPROVED** — owner authorized via autonomous-delegation directive (session 2026-08-02: 'work autonomously through, never prompt, always continue') | bsp-dungeon | Phase 01 RichnessV1 contract amendment (2026-08-02) | 2026-08-02 | The Richness V1 sprint requires an authorized contract before implementation. Freezing the contract in specifications now — with explicit PENDING OWNER status — allows the sprint to proceed with a clear target while preventing premature implementation. The contract is additive: no baseline v3 behavior, profile, tag, or corpus entry is changed. | Proceeding without a frozen contract would risk scope creep during implementation. Authorizing implementation code in Phase 01 would violate the sprint's phase gating. | No Richness implementation code exists. `from_tag("richness-v1")` must return `None` until owner approval. All RichnessV1 acceptance rows in `bsp-acceptance.md` §18 are marked PENDING. See `bsp-dungeon-generation.md` §21 for the full contract. | `bsp-dungeon-generation.md` §21, `bsp-acceptance.md` §18, `bsp-compatibility.md` §14, `bsp-spatial-physics.md` §11, `bsp-renderer-lighting.md` §13, `bsp-transaction-ownership.md` §16, `api.md` | none | 2026-09-02 |

## Pending Owner Evidence — DECISION-20260802-01

**Status: APPROVED.** Owner authorized acceptance of the regenerated baseline-v3
manifest and the RichnessV1 amendments through the autonomous-delegation
directive recorded in DECISION-20260802-01/-02 (2026-08-02 session: 'work
autonomously through, never prompt, always continue'). This block does not
behalf.

### Authority and mismatch disposition

- **Old manifest:** `enhanced-v3-corpus/v1`, SHA-256
  `047f637060df8ecbd5132352923e15f6cbca4039662cfe7b9c4cd283c52c0784`,
  from task-base predecessor `71c507d8b906d3cd2396a9875ad733acbef7f609`.
- **Task-base implementation:**
  `8203a7f84c1a8ef2952aa06634469b68dab7d417`
  (`fix(enhanced_v3): route overlap safety, guaranteed rectangles,
  multi-stair layouts`). The focused test records this Git identity and the
  full pre-freeze `git status --porcelain=v1` in
  `baseline-freeze-provenance.json`; it never selects substitute seeds or
  modifies generator code.
- **Mismatch explanation:** the task-base repair made chamfered-room
  assignment deterministic (every fifth room is rectangular) and made the
  placement grid stair-aware. Therefore all 12 task-base map, metadata, BSP,
  and LIT artifacts differ from the historical manifest. The proposed values
  below are task-base outputs, not a normalization of a generator mismatch.
- **Proposed manifest:** `enhanced-v3-corpus/v1`, SHA-256
  `85da8f8888fcaccb58eaca94f6ea94da71adc12d9d1efcde355c5fd9ef3bcfce`.

### Compiler provenance

| item | recorded value |
|---|---|
| compiler | ericw-tools 2.0.0-alpha3 |
| qbsp SHA-256 | `4a05974acf9e59f73a9c8f4e8236f3d1e0961be477dae002837e166278882f17` |
| vis SHA-256 | `f7f429e0ad9bebbb0ebdefea8d6cd5e13a6ad6ff9f6893126772e00b66e364ea` |
| light SHA-256 | `1210ee9bed8990f67e3be7e28fbfd8d210329b052ac57dba017200d2da1ca5e5` |
| cc0_dungeon_v2 WAD SHA-256 | `b899ae8165744fdc2d92f9fe4d03d5b52c33ebfddc228822c70790e120cd3496` |
| palette SHA-256 | `1fc70b9759784bbab3fb0af1e644e15565fd3f3062dea50d9c96703e1194b9a2` |
| exact command vectors | `qbsp -bsp2 -threads 1 generated.map`; `vis -threads 1 generated.bsp`; `light -threads 1 -lit generated.bsp` |
| compiler boundary | two independent temporary roots per cell; warning, skipped-fill, `.pts`, `.leak.prt`, BSP2 magic, and strict-load diagnostics fail closed |

### Complete old-versus-proposed artifact hash table

| cell | old map | proposed map | old metadata | proposed metadata | old BSP | proposed BSP | old LIT | proposed LIT |
|---|---|---|---|---|---|---|---|---|
| v3-sparse-seed-0 | `2f61feb27abe698e5b07dbbd49d51e505c025f77bb6d83df766ebd6f9ec9ec4a` | `de1a15491ffeb4d3aaa2f1e833198b9f133faf561ffaa845fda47db242be7ccd` | `4834630c434110f808f930a7237473079a199d6605ac4ba0ee50fe6199c2df57` | `78ef3408583b1e106b81a0319d387ec97b4cd2dd16b7cc875239aecc8913ff71` | `86b785af2d712d8b07636060f624db2f955b66cc96d138531197fa03321cdee9` | `172245b581f54d73d026dd0806fc56a0ba53e6f561bf9153437a5e60bae607b0` | `f4b41a5c2990710213bc891776c21e1c1cabc540c22b94266d319f81293aa784` | `9c334f0b520c83085d818accd2dd85fdae5a84f2b3be3a62e6070585ad3c3946` |
| v3-sparse-seed-42 | `ea00d65b7a2d2bbe408a94479245af8d992a47760e3b2f27a12a200f3ef40ec8` | `8f43a8ea036d494a44d480d49d0150661ce9091c54f8dbcbe6483548c4e37534` | `8e89b0bdca12b0d098b147ceaece6ea5d50f49684abf6bff202386a63011f25e` | `0d94ebd6c9db0e2752186f2ce10dc5cb04aa7971847464609ae3fa5c712251da` | `fb7ad85f8053b60c2741b8d541cd20192620413829666e7a7fed6412c52fce9b` | `aca7aa3064a1a044479284daf35ba4cd8f496ec235fa52477e2d0325a05312d3` | `b4adad4d63461a142ac6b6a5d11bb87d0ebefab15fea033d69b3be6959983012` | `42690dfdbd644b5ee0660d70519cc982c20e24437492ba59f8b228a5d05aff2a` |
| v3-sparse-seed-99 | `6bcbd3074dd89c0d5dad339d920c017188d935d6b477cc16dc59dfa56b67b9c0` | `d575d38c8be33c67bfea1d1735ca06b3bf07efcadd72f6ef440b18f1520e8342` | `b640dbcff73a06b05fc6c47fb7d984279442bd715bf73050925ab61fa02c91c2` | `d9354dbb474f80a0bee4619ef25d4196ff0a32b233a1925f6c9e2aac14faf76a` | `f74864d2df9e6d9b4eda8dac15fd98bb64da2b40c9b14a81fffd3340d350fa10` | `d0ae612a724deceda4eb8c76e9d6f0a8f94e836b4cd1031fa0b3b8309c18d7e9` | `0dcbab5fe189353d6c717af814f903520f1830b9b346cad388423563c2d8f1a2` | `fa5e14cd9d0486b59242b85c9ff168bac375a969a02b7493ca25eee1dc7f5929` |
| v3-sparse-seed-255 | `bb56a914a13b0d58b484cd4be3d57574e8cd21743f28d13ab1b2cd81b0f9cdfd` | `015d52a17c3d748b804c6f433953a6ba9be3750775faf4699fb506adeff4ffbf` | `8e1f0614a24759226e1731301ffc2a2a244b89f8b7d97511a9a1656e747b177c` | `ee2bf5db3543cc6934b56972d642e0f523afd9507bb90021861c98aaabf57d2e` | `169be215cc0e9aed16ac2dbcb6e1cebbd90a028bbea6de302c2793c86f27bd2f` | `0f5235b94a8c53c2211e7ff008802642f75f361d46910e70ad01a142abfee596` | `6aac50dcde3534dde566f14dabbe0b4d6d077e94924719347a93896803762e22` | `5cc5ed33eefe466f7917f10a5e4be9802ab6a7c32662d8f87bc4c5cf1415a3da` |
| v3-moderate-seed-0 | `a210e863be58f358c0f9be3659c29ca2b434a5d19ea7a383e1d964ab68811199` | `68283a728a6e4a7bc72e088eb02baab32a1b61e6d9c2af10db23e935e1bf754d` | `8545af5609654450d210ea7dc04424f57a943f54ef70114a370db6f3b14f3189` | `b8370b09c1d44f5c1298667a55b3f692471018f0c7530e6e9e61ab577db3d912` | `e4d535bda0147ce9b799fbc531755b026f0d0f4e9eca6aad4a8aa6a67ede65ba` | `bddab063f811898f9771ff424de07445d61975736ac4c12ec7d5d13f3cb16db1` | `9332ca63ebe828871005919b454f71bc242e9b0f00a34c0c13f28201ccdd86e3` | `ed3347b5dff88fa0cfa04c0616d8869396fcd71163bc700eacdadfc084180b8c` |
| v3-moderate-seed-42 | `3dba4256903528f1b7671d589b0737370ed81fbabec50c85b03dd4d47f140c34` | `85cdcd7caa80dc4fbd9bc8d12d1089ef50278f6e689de89519c1a384dacb6d66` | `5c3854c00dec07beeb61de713027dd7959034f9790da54a71ff44413d7031799` | `644b2003c0608ecfd9813b0fe8f0c08116b094b14ed4f6dea886331e62862a99` | `10d4a3b9288be326ae706a6d1ed1029b9fcbce56fa0a4bf1ef54be9718daf16f` | `1ee474de9ebc315a3c90477cf509764d1e67f32e33d280d1c8aac5bba0df9547` | `1f47515ebfe7e77714200a5daeffec404c84b8c748fc7f403b38be0a590a6be3` | `b14604b1699f044f0a0e31d65a9f5c524a6a5aedea71b46e7b92b51a70749cfb` |
| v3-moderate-seed-99 | `3054d5cda49393e10cb7bcd2382e4b4151901da1d99ced06bf5169e88d9b5efb` | `7ff03faf1755ca320a3a9cd22163ea775cca6bc1f51d9907915d5be0639e61b7` | `886110e2c8c3a42a31a6e5df265a62dea8d3265c969c81a2bc166483c2ad6498` | `c425f353e30a4ef1e7167591eef140a6a05008cdb4fbaadabc882f6c3b2ee461` | `ed8d8143b2acd3c3099dca9bfc8507722be833f7a9bca8ee3a986e4def45b17f` | `7737924358ea1cf55a051d554708dc341d35b149625cc7b25fe52ddd780fd9c1` | `178bca0be54581876ce52a98fbb33d40d4c69c229eb6aea6e0adf86c6cfb4d95` | `06f9ca5845ec43a17c3759ac53791222ef58544103acf30baf7459336235d507` |
| v3-moderate-seed-255 | `27344f2467a417924078390286e550817fed8a88d02050a52a3ddb2ce6707ee8` | `f6cc66daf8e6f5d2ee7c12c1acff90a4590417c648eb8e2eb036a119cb62d6af` | `03f44491ac366b23531262c7ceeab5a07e00272a16392711a12ee2d91c116d32` | `28fda28eda1e8d5b97b3c1412650fc2cd37995e4585c48f7927a4e497ab20de1` | `bfe6a6f3e508c1c2837d163288b8f1c114cd23f1a018d4c9215825985e9e7a9a` | `82a5760453977e910901e4555a4ac055801524a0d2b5c8ffbc48a15f8845d31c` | `6c9f4a725a475275b70eed2f59f7b866c6196980113d244289027d534de591db` | `ba4831257306687f3438322d75934071002cbc789bc2a02a7170a11834149d9c` |
| v3-rich-seed-0 | `147b6793c9ccb15ee9c69e694ce491caa2b950041ebeca64827545fa296bbfa6` | `878fd2734457fa5c056b9273d1ff4c0331aad4c165b0ed72872655bb87b532bc` | `30d7c6415b191a1106faa19cfa28c7aced02373c53a15c1dd86e1213b83773ff` | `289b15cac284822cb424fb2cdee5351909bd989c7bdc22cc1a48b0e0556d365e` | `edaeac830b277c4aaa16a23c933f42c71695db656634b539896eefe43497cede` | `800b99f665f753734554a867e764c311df0a48cb488919e8d4702fd2be71db2e` | `cb76e0893e8e61205ab0bc0c0cbcffaa701df85b1bbe79ed8ddd468a01d96b86` | `2bab2bba3d3e73cef846f683071e879736a67fc1823d79b6b478cebe4b7abdfc` |
| v3-rich-seed-42 | `c7cf97278c31e4a3cbc435b843016b07c9343939033098b5dc9e92fe16d37bf5` | `ef9315d3bfae415be097fb823583afcdd6d1408ea1ab9f60bc2bf561b6181bf2` | `422c3fd9f594a76c35c8ad7dba1e831db8344ce2bb9fac41c2e03ca8f55c7a1f` | `5b81ece2fb3d7406c46e7c2afd5df45b646972e646b8647a57aa28710b54415c` | `e13b0f192e936c87f7981032cc78d5a0484811f74b16079469b611895ba28703` | `c28752c1be51dfdf3688f4c7af1d4fc5b38b588dbb124ac215c1d7ae1ff9a6ec` | `3abef4e47a6a5cdbf495965004d74335a04f1c01d7c9910c16ff9cd6af8103f7` | `44f59f0d532f8335561639e8ba46ddd192e0b39a0ead5eef6acb092ece196dab` |
| v3-rich-seed-99 | `d318b9c345937ffa3af937ab348e2a88c80d1a18af165b88f37dda91644918b1` | `d729dcccc16a07b1c08b3766584a77a7853e854e09a8ed5684e358e6d724c032` | `cd1c7b997208450889a2f615eb8d92e7472980b8c354a3bb0a897b301c4015aa` | `98f5b6ebf59ced453917115fe15889dc80d2a43d0063d0e214bfd27736ff859c` | `d61aff8bf05aedf974df7ec7be9a3f8b3d545b020345d5959de9c33d6e658e76` | `a9f3def805e3638f9f0ca3945eee853a37e888e20114c3acf07fcc1e37f32ee1` | `6eb3ba4414fd72c79f1def66f964e32f5dc412076d2c6cafd51285676c58baf3` | `4527b579af4108db4f9ba48b8165a94ce692876dc3f60c430209c706908526b1` |
| v3-rich-seed-255 | `8e1d3b3459db2569db7f4c5445a824107152bc518c93b568b712e2e5b3150104` | `f27a96e4791e34abc07518601245c2e14c3d3ad590b35cc392c6ab104c3df637` | `81eb4d896e608347ddf32f03e6ce3cc58a694a8367c5ea330087b87b3e37d4cc` | `2724ac843f814f03198e30e1aafa9ece807e177f05d1d753916e5b67e4916ed4` | `5be07559746941c0de6031f76d71b6c033e07b1c1e858fe41e857cbf0b82447c` | `223b16f45adebf6c55b6f76532182e5132b87d2cccf5fe95f942f5df6f3f03fc` | `601c11745d114caea3cb06176ed3085a474a8410ceb9719f63cabf844843cb67` | `b9b478d243b05234baedd3a509d9402f0cbb9f2bdc2918c55aef283ff7311280` |

**Owner decision required:** accept these proposed hashes as the baseline-v3
manifest and approve or reject the linked RichnessV1 living-spec amendments.
The recorded autonomous-delegation directive authorizes DECISION-20260802-01
and DECISION-20260802-02 as APPROVED (2026-08-02).

## Superseded Decisions

| id | decision | status | owner | source | decided_on | superseded_on | superseded_by | knowledge_ref | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DECISION-20260724-10 | Emit all generated dungeon geometry from one deterministic 16-unit room/corridor open-space union. | superseded | bsp-dungeon architecture | user-authorized BSP portal and clearance repair | 2026-07-24 | 2026-07-25 | `DECISION-20260725-16` | `bsp-generator-compiled-spatial-validation.md` | Runtime capture exposed broad scene-spanning slabs and role-obscuring side faces; the corridor-only hybrid retains the valid additive-clearance property without merging room shells. |
