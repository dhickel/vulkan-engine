# Alpha Baseline Residual Register

Date: 2026-07-03

## Scope

This register consolidates Sprint 01 baseline residuals from `docs/gap-report.md`, the Phase 01 baseline inventory, the Phase 02 docs drift audit, validation reports, root/package manifests, and targeted live-source inspection. It is a classification artifact, not a product-fix plan.

Allowed statuses:

- `verified_current`: live source/docs confirm the issue still exists.
- `stale_resolved`: old claim is contradicted by live source.
- `unknown_needs_audit`: cannot be verified cheaply during Sprint 01.
- `accepted_alpha_debt`: known limitation is accepted for a future sprint.
- `blocked_validation`: validation or evidence could not run and must be retried.

## Findings

| ID | Residual / historical claim | Status | Evidence | Impact | Next action | Likely sprint / track |
|---|---|---|---|---|---|---|
| ABR-001 | Historical claim that scene serialization/save/load support was missing. | `stale_resolved` | `src/renderer/src/api/scene.rs:699` defines `Scene::save`; `src/renderer/src/api/scene.rs:713` defines `Scene::load`; `apps/editor/src/main.rs:557` and `apps/editor/src/main.rs:577` wire editor save/load actions. | Do not plan from the old missing-serialization claim. Current work should validate quality and coverage instead of adding the feature from scratch. | Keep future work focused on round-trip coverage, package asset references, and editor UX hardening. | Track B; Track C |
| ABR-002 | Historical claim that ray picking and object selection were missing. | `stale_resolved` | `src/renderer/src/api/scene.rs:755` defines `Scene::pick`; `src/renderer/src/api/scene.rs:780` defines `pick_last_camera`; `apps/editor/src/main.rs:396` handles viewport pick and selection. | Picking exists, so future work should validate precision and UX rather than treat picking as absent. | Add visual/editor workflow validation when editor placement sprint claims picking readiness. | Track B |
| ABR-003 | Historical claim that undo/redo infrastructure was missing. | `stale_resolved` | `src/renderer/src/scene/command.rs:1` documents command-pattern undo/redo; `src/renderer/src/api/scene.rs:807` exposes execute/undo/redo; `apps/editor/src/main.rs:539` and `apps/editor/src/main.rs:546` wire undo/redo actions. | Undo/redo exists for command-backed mutations, but coverage and command breadth remain sprint-specific concerns. | Validate command coverage per editor workflow before advertising broad editor undo/redo. | Track B |
| ABR-004 | Historical claim that headless/offscreen renderer support was missing. | `stale_resolved` | `src/renderer/src/api/renderer.rs:128` defines `Renderer::new_headless`; `src/renderer/src/api/renderer.rs:368` defines `render_scene_headless`; `src/renderer/AGENTS.md` documents headless capture validation. | Headless support exists and is the preferred validation path for visual renderer work. | Require capture evidence only for visual behavior changes or visual-readiness claims. | Track H; Sprint 01 Phase 04 validation |
| ABR-005 | Audio subsystem exists but alpha readiness is not proven by workspace membership. | `verified_current` | `Cargo.toml` includes `src/audio`; 7 tests pass (1 ignored, manual device smoke); Sprint 07 delivered audio foundation with clip loading, playback, and validation. Phase 02 (gate review remediation) added `engine_events` dependency and `From<AudioClipId> for engine_events::AudioClipId`. | Audio crate is functional within alpha scope. Remaining limitations (no spatial audio, no 3D positioning, no channel groups) are documented alpha debt, not missing features. | Future sprint: spatial audio, channel groups, editor integration. | Track F |
| ABR-006 | Physics subsystem exists but alpha engine-level collision is not proven by workspace membership. | `verified_current` | `Cargo.toml` includes `src/physics`; 11 tests pass; Sprint 06 delivered physics foundation with body/collider creation, ray casting, contact records, and event bridging. Phase 02 (gate review remediation) extracted shared `string_id!` macro and added `From` impls between crate-local and `engine_events` ID types. | Physics crate is functional within alpha scope. Remaining limitations (no joints/constraints, no scene integration) are documented alpha debt. | Future sprint: joints, scene integration, dogfood migration. | Track E |
| ABR-007 | Scripting subsystem exists but safe engine bindings and workflow are incomplete. | `verified_current` | `Cargo.toml` includes `src/scripting`; 9 tests pass; Sprint 08 delivered scripting boundary with `ScriptEngine`, `ScriptContext`, and Rust app crate extension path. | Scripting is experimental within alpha scope. Do not imply scene/physics/audio mutation support or hot Rust reload. | Future sprint: sandboxed bindings, hot Rust reload, scene/event scripting. | Track G; Track D |
| ABR-008 | Editor project and asset-browser readiness was historically uncertain. | `verified_current` | `apps/editor/src/main.rs:311` loads project context; Sprint 03 delivered editor placement with save/reload and headless capture evidence; Sprint 05 added event system integration. | Editor project/package workflows exist within alpha scope. Remaining limitations (browser UX, validation errors, advanced transform editing) are documented alpha debt. | Future sprint: editor UX hardening, material graph, CSG editing. | Track B |
| ABR-009 | Production-path error propagation remains a historical candidate. | `unknown_needs_audit` | Sprint 01 only inspected targeted docs and representative source paths. **Gate review (2026-07-03):** Sprint 12 classified 150+ unwrap/expect sites; ~15 high-priority sites deferred for error hardening. AGR-023, 043 address descriptor pool exhaustion and bare `.lock().unwrap()` sites in remediation. | Error path hardening is tracked but incomplete. | Continue unwrap→Result conversion in dedicated error-hardening sprint. | Track C; Track H |
| ABR-010 | Swapchain rebuild cleanup remains a historical candidate. | `unknown_needs_audit` | No resize/swapchain runtime smoke was run in Phase 03. **Gate review (2026-07-03):** Sprint 12 Vulkan lifecycle fixes (fence leak fix in vk_types.rs, Drop panic fix in vk_render.rs) were source-reviewed. Swapchain rebuild behavior requires runtime validation with Vulkan validation layers. | Resize behavior can affect editor and app stability. | Validate swapchain rebuild with Vulkan validation layers post-build. | Track H |
| ABR-011 | Animation/skinning runtime completeness remains a historical candidate. | `unknown_needs_audit` | `src/renderer/src/animation/mod.rs:1` contains an animation module, but Phase 03 did not validate loader/runtime/render integration. | Animation may be partially implemented but should not be advertised as alpha-complete. | Audit animation import, playback, GPU skinning, and examples before making alpha claims. | Track H |
| ABR-012 | Editor camera workflows remain a historical candidate. | `unknown_needs_audit` | Editor source contains viewport and tool wiring, but no Phase 03 visual/browser validation was run. | Camera usability affects editor alpha readiness. | Validate camera controls in the editor workflow sprint with capture or interactive evidence. | Track B |
| ABR-013 | Asset hot reload behavior remains a historical candidate. | `unknown_needs_audit` | `src/renderer/src/data/file_watcher.rs` exists, but Phase 03 did not trace live reload integration or runtime behavior. | Hot reload claims need runtime evidence and clear failure behavior. | Audit data/asset hot reload separately from packaging and app dev-loop claims. | Track A; Track C |
| ABR-014 | Depth buffer access for hooks/debug remains an open source-level limitation. | `verified_current` | `src/renderer/src/api/renderer.rs:806` and `src/renderer/src/api/renderer.rs:817` pass `None` with `TODO: plumb depth texture from rendergraph`. | Advanced hooks/debug views cannot currently consume a depth texture through that path. | Track as renderer facade/debug debt when advanced hook or capture work needs depth. | Track H |
| ABR-015 | Public rendergraph extension access remains not alpha-stable. | `accepted_alpha_debt` | `src/renderer/src/rendergraph/mod.rs:34` exposes `RenderGraph`; `src/renderer/src/rendergraph/mod.rs:44` exposes `RenderPassNode`; Track H says advanced rendergraph/pass APIs need explicit resource and ordering contracts. | Some internals exist, but alpha users should not infer a stable extension API. | Keep raw/advanced rendergraph APIs marked unstable until resource/order contracts are documented and tested. | Track H |
| ABR-016 | Critical test coverage remains a historical candidate. | `unknown_needs_audit` | Phase 03 did not run compile/test suites beyond documentation/process validation. The validation matrix routes compile, input, renderer examples, runtime smoke, and capture checks to Phase 04/future sprints. | Coverage gaps may exist across renderer/editor/support crates. | Phase 04 should establish current compile/test baseline and record blockers exactly. | Sprint 01 Phase 04 |
| ABR-017 | Push constant sizing remains a historical candidate. | `unknown_needs_audit` | Shaders contain push constant blocks, but Phase 03 did not inspect Vulkan layout limits or device-specific behavior. | Incorrect sizing could break renderer portability. | Audit shader/Vulkan push constants in renderer internals work, with device-limit evidence. | Track H |
| ABR-018 | Example asset-path ergonomics remains a historical candidate. | `unknown_needs_audit` | README and root stub route users to renderer examples; Phase 03 did not run examples or audit path behavior. | Poor paths can block first-run developer experience. | Phase 04 runtime smoke should record example path blockers; future launcher work should unify project paths. | Track C |
| ABR-019 | Legacy scratch modules remains a historical candidate. | `unknown_needs_audit` | Phase 03 did not perform a whole-repo dead-code/scratch-module audit. | Scratch code can confuse alpha users and docs. | Run a targeted module inventory before cleanup sprints; avoid deleting code during baseline classification. | Track H |
| ABR-020 | Input profile end-user schema documentation may be incomplete. | `unknown_needs_audit` | `src/input/AGENTS.md` names strict profile serialization/parsing; `src/input/src/lib.rs` implements profile parsing and TOML load/save, but Phase 03 did not audit user-facing docs coverage. | Missing schema docs can block app authors using action maps. | Audit `docs/api/06-input.md` against `src/input/src/lib.rs` before public alpha docs claims. | Track D |
| ABR-021 | Frustum and occlusion culling historical claims need separation. | `unknown_needs_audit` | `src/renderer/tests/integration.rs:91` tests frustum AABB intersection; Phase 03 did not verify runtime culling integration or occlusion culling. | Culling performance/readiness should not be overclaimed. | Audit runtime frustum integration and decide whether occlusion culling is future debt. | Track H |
| ABR-022 | Phase 02 validation included commit/push/email gates outside this worker's authority. | `blocked_validation` | User supplied Phase 02 docs commit `639dffd420ff5962327e0c15b5885c9034283ab5`, validation remediation commit `4294788d6cd24ca25eba742f6fff9728767144f5`, and email thread `f909ab8f-7047-4e80-955b-7d8e62aada7d`; `git log` confirms current local and remote HEAD at `4294788d6cd24ca25eba742f6fff9728767144f5`. | Phase 03 worker can validate local artifacts but must not claim it performed commit, push, or email gates. | Parent/main thread should own Phase 03 commit, push, validator, and email closeout. | Sprint 01 orchestration |

## Risk Assessment

The old gap report can no longer be used as a raw defect list. Several missing-feature claims are contradicted by live source and are now `stale_resolved`, while support crates/apps are better treated as `accepted_alpha_debt` until specific alpha workflows are validated.

The main remaining risk is overclaiming readiness from existence. Workspace membership and a thin crate are not enough evidence for alpha completeness.

## Recommendations

- Use this register as the routing table for Sprint 01 Phase 04 and later deep-track planning.
- Keep `unknown_needs_audit` items out of current-defect lists until a targeted audit verifies them.
- Require visual/headless capture evidence only when a sprint changes renderer-visible behavior or claims visual readiness.
- Preserve the public docs wording that points to current alpha readiness and this residual classification route instead of reviving the old raw gap list.

## Follow-ups

- Phase 04 should run the compile/test/runtime baseline checks from the validation matrix and update `validation-summary.json`.
- Future track planning should convert accepted alpha debt into scoped sprint acceptance criteria, not broad "make engine alpha-ready" work.
