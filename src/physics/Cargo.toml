[package]
name = "physics"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_events = { path = "../events" }
rapier3d = "0.21"
rapier2d = { version = "0.21", optional = true }
serde = { version = "1", features = ["derive"] }

[features]
default = []
rapier2d = ["dep:rapier2d"]

[dev-dependencies]
serde_json = "1"
