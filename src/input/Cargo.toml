[package]
name = "input"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_events = { path = "../events" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
winit = { version = "0.29.15" }
