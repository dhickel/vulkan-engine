//! CSM visual validation capture test (stub).
//!
//! This example provides the entry point for CSM capture validation.
//! Full capture harness requires the renderer runtime and is exercised
//! via the headless capture workflow.
//!
//! Run with:
//!   cargo run -p renderer --example capture_csm --features csm -- --headless

fn main() {
    eprintln!("capture_csm: CSM capture validation entry point.");
    eprintln!("Use headless capture workflow for visual evidence.");
}
